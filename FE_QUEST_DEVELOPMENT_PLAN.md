# FE QUEST 開発計画 / 引き継ぎドキュメント

> **このファイルは FE QUEST 開発の Source of Truth（引き継ぎ元）です。**
>
> 別チャット・別AIで開発を再開するときは、最初に GitHub の `main` とこの `FE_QUEST_DEVELOPMENT_PLAN.md` を確認してください。会話履歴や古い記憶より、GitHub の現行コードと本ファイルを優先します。

最終更新: 2026-08-22  
現行アプリ: **v337**  
リポジトリ: `taiwanwan64/fe-quest`  
次の計画バージョン: **v338**

---

## 1. プロダクトの北極星

FE QUEST は、基本情報技術者試験の単なる問題集ではなく、**学習者の実際の状況から「今日やること」を自動調整し、合格までの学習運転を担う適応型学習アプリ**として育てる。

最も強く残す価値は次の一連の接続である。

**教材 → 科目A演習 → 復習 → 弱点補強 → 科目B → 模試 → 学習計画 → 直前期調整**

現在の実装では、試験日、1日の学習時間、直近の学習ペース、問題ごとの正誤・所要時間、忘却状態、誤答理由、教材進捗、科目B進捗、模試履歴などを使い、日々の学習配分・復習・弱点補強・直前期の負荷を変えている。この適応学習ロジックは FE QUEST 最大の差別化要因なので、今後のリファクタリングでも**挙動を壊さないことを最優先**とする。

商品としての中心メッセージは次の方向を維持する。

> **計画どおりに勉強するのではなく、勉強した結果に合わせて計画が変わる。**
>
> あなたが勉強する。FE QUEST が、次の一手を考える。

---

## 2. v337 時点の評価と判断

第三者AI（Claude）に、v337 の完成版 `index.html` 約3.67MB / JavaScript 約19,771行を実際に解析してもらった。総合評価は **76/100**。重要な評価は以下。

### 高く評価された点

- 自動調整は見せかけではなく、複数シグナルを統合した実用水準の適応型学習。
- 試験までの日数に応じた複数フェーズ、直近実績ペース、忘却曲線、弱点、誤答理由、科目B進捗、模試履歴が実際に学習計画へ接続されている。
- 710問、130テーマ、科目B演習などのコンテンツ量が実装と一致している。
- localStorage中心のPWAとしては、チェックサム、atomic保存、last-known-good、migration journal、IndexedDB復旧点、複数タブ競合防止などデータ保護がかなり強い。

### 主な課題

- `override` の積み重ねと関数の多重ラップが増え、技術的負債が顕在化している。
- 本番runtimeに旧来の hard `assert()` が残り、将来のコンテンツ変更で起動障害につながる理論上のリスクがある。
- 完成版が単一 `index.html` 約3.67MB まで巨大化している。
- 初回ユーザーが試験日・学習時間を設定しないと、最大の差別化である自動調整価値が伝わりにくい。
- 誤答理由は自己申告依存が強く、実測データとのクロスチェック余地がある。
- 商用化にはアカウント・クラウド同期・課金基盤が不足している。

### この評価を踏まえた開発判断

**クラウド同期をすぐに追加しない。**

理由: 現在の localStorage / IndexedDB / migration / recovery / multi-tab conflict に、さらにクラウド同期と同期競合を重ねると、技術的負債が最も大きい状態で最も複雑な状態管理を追加することになるため。

したがって、今後は次の順序で進める。

1. 内部構造を把握する
2. 挙動を変えずに安全性を上げる
3. 初回・日常UXを仕上げる
4. 配信構造を分割する
5. その後にクラウド同期へ進む

---

## 3. 開発の基本ルール

今後の全バージョンで以下を守る。

### 3.1 学習効果を壊さない

リファクタリング時も、以下の主要挙動を回帰テストで保護する。

- 710問の問題バンク内容とID整合性
- 130テーマの教材構造
- `buildTodayTasks()` 系の今日の学習生成
- 通常期と直前7日の分岐
- 復習期限・忘却状態
- 科目A / 科目Bの配分
- 模試結果から readiness への反映
- 学習履歴・XP・ストリーク・各種進捗保存
- 復旧センター、last-known-good、migration、複数タブ競合
- PWA更新・オフライン起動

### 3.2 大規模一括書き換えをしない

「きれいにするために全部作り直す」は禁止。既存挙動を固定したテストを先に用意し、責務単位で段階的に置き換える。

### 3.3 UX改善は止めない

内部整理期間でも、日常利用で見つかった明確なUI不具合は修正する。ただし、小さな不具合のたびに新しい永久overrideを増やすのではなく、可能なら整理対象の本体側へ吸収する。

### 3.4 新機能より既存価値の完成度を優先

v338〜v342の間は、睡眠・体調入力、バッジ大量追加、SNS機能など大きな新機能は原則追加しない。

### 3.5 「合格確率」と誤認させない

現在の readiness は FE QUEST 独自の「受験準備の仕上がり」指標であり、合格確率ではない。将来60%基準などを表示しても、公開模試・演習上の目安として扱い、実試験の合格予測と断定しない。

---

## 4. 次の5バージョン

# v338 — 技術的負債・実行経路の全体監査

### 目的

**何を直すべきかを、推測ではなく実コードから確定するバージョン。**

この段階では原則としてユーザー向け挙動を変更しない。

### 実施内容

- versioned override ファイルの一覧化
- 同じ責務を複数バージョンで上書きしている箇所の抽出
- `refreshProfileUI` など多重ラップ関数のラップ深度・呼出順を可視化
- runtime hard `assert()` の全件棚卸し
  - 起動必須の安全チェック
  - CIへ移せるデータ品質チェック
  - 既に非破壊contractへ移行済みの重複チェック
  に分類
- `index.html` 完成版の容量内訳を計測
  - CSS
  - app logic
  - 科目A問題
  - 教材
  - 科目Bデータ
  - diagnostic / legacy / release用コード
- 起動時に毎回実行されている検証処理のコストを整理
- 自動調整主要関数の「現行挙動契約」をテスト化
- 今後統合するoverrideと「互換性維持のため残すoverride」を分類

### 成果物

GitHubに `audits/post-v337-architecture-audit.md` 等の監査結果を保存し、v339以降の作業順を確定する。

### 完了条件

- hard assert の正確な数と役割が把握できている
- 多重ラップ上位の主要関数が特定されている
- override統合候補が依存順付きで一覧化されている
- v337と同じ学習挙動を判定できる回帰fixtureが用意されている
- ユーザーデータスキーマには変更を入れない

---

# v339 — runtime安全化 + override整理 第1弾

### 目的

**最も危険度が高く、挙動を変えずに除去できる技術的負債から減らす。**

### 実施内容

1. 本番runtimeの hard `assert()` を原則廃止
   - 問題数、カテゴリ配分、認知レベル分布などビルド時に検査できるものはCIへ移動
   - runtimeで必要なチェックは「起動停止」ではなく非破壊diagnosticへ統合
2. 多重ラップ関数の整理 第1弾
   - v338監査で最もラップが深く、日常UIに影響が大きい関数から対象を選ぶ
   - 単一実装 + 明示的hook方式へ寄せる
3. v134〜v140等の科目A個別override群を監査結果に基づき統合
   - 問題本文・正答・学習意図を勝手に変更しない
   - 同一目的のパッチを一つの正規データにまとめる

### 完了条件

- ビルド時品質検証はCIで維持される
- runtime hard assert によりアプリ全体が起動不能になる経路を原則0にする
- 対象関数について不要な多重ラップが減る
- 問題バンク・教材・科目Bの意味的差分がない
- v337基準の主要ユーザージャーニーが全て通る

---

# v340 — 初回体験 + 日常利用UX完成度向上

### 目的

**初めて使った人が1日目から「自分用に学習計画が変わるアプリ」だと理解できるようにする。**

### 実施内容

- 初回オンボーディングを再設計
- 試験予定日と1日の学習時間を最優先で設定させる
  - 自動調整を利用するには強く設定を促す
  - 不要な長い説明は避ける
- 入力直後に「あなたの今日の計画」が生成されたことを視覚的に見せる
- 「なぜこの学習なのか」を短く説明できるUIを検討
  - 例: 「科目Bが予定より遅れているため +10分」
  - 例: 「昨日間違えたネットワークを復習に追加」
- 全画面横断の日常利用UX監査を継続
  - 余白
  - 情報量
  - 主CTA
  - スクロール位置
  - 固定ヘッダー / 下部ナビ
  - タップ領域
  - iPhone実機表示
- 1日目 / 3日目 / 1週間の離脱要因をシミュレーション

### 完了条件

- 新規ユーザーが数十秒以内に試験日・学習時間を設定できる
- 初回から「普通の問題集との違い」が分かる
- 主な画面で「次に何を押すか」が1つに絞られている
- 既存ユーザーの保存データ・設定を壊さない

---

# v341 — 配信構造・巨大HTMLの分割

### 目的

**3.67MB級の単一HTMLに依存する構造を段階的に解消し、初回ロード・保守性・将来のクラウド化に備える。**

### 方針

PWA・オフライン対応は維持する。単純に外部CDNへ逃がさない。

### 実施候補

- static shell / CSS / app logic / content data を責務で分割
- 科目A問題・教材・科目Bコンテンツを外部静的asset化
- 初回に不要なコンテンツを遅延ロード可能にする
- Service Workerで必要assetを安全にcache
- コンテンツmanifest / versionを導入し、app schema versionと分離
- GitHub Pagesでも動く構成を維持

### 数値目標

- `index.html` のサイズを大幅に縮小する
- 可能なら初期HTML 1MB未満を目標とするが、数値達成より**起動速度・オフライン性・壊れにくさ**を優先
- 初回起動に不要なデータを一括parseしない構造にする

### 完了条件

- オフライン再起動可能
- v340以前の保存データをそのまま読める
- 主要学習画面の体感劣化なし
- asset欠損時に破壊的エラーではなく復旧導線がある

---

# v342 — アカウント / クラウド同期 基盤

### 目的

**端末変更・紛失・ブラウザデータ削除でも学習履歴を失わない商用サービス基盤を作る。**

ここで初めてクラウド同期へ進む。

### 実施内容

- 認証方式の選定
- クラウド保存先の選定（Supabase等を第一候補として比較するが、実装前に費用・セキュリティ・運用を確認）
- 現行のbackup payload / profile schemaを利用した同期API設計
- ローカルファーストを維持
  - 通信がなくても学習可能
  - 通信回復後に同期
- 同期競合のルールを明文化
  - revision
  - updatedAt
  - device/session識別
  - 同一項目の競合時方針
- 初回ログイン時の既存ローカルデータ取り込み
- サーバー障害時でもローカル学習を止めない
- ユーザー自身によるJSONエクスポート / 復旧センターは残す

### 完了条件

- 端末Aで学習 → 端末Bへログイン → 正常に続きから再開できる
- オフラインで学習 → 再接続 → データが失われず統合される
- 誤った古いクラウドデータで新しいローカル進捗を上書きしない
- ログアウト・アカウント削除・データ削除方針が明確
- 個人情報・プライバシー方針を一般公開前に用意できる状態

---

## 5. v343以降の候補

v342で商用基盤が安定した後に着手する。

### v343 — 適応学習の精度向上

- 誤答理由の自己申告と実測回答時間をクロスチェック
- 「時間不足」「知識不足」「読み違い」等の矛盾を補正
- 確信度入力は、操作負荷を増やさない方法に限定して検討
- 週次の伸び・減速を自動調整へ利用

### v344 — 学習成果の見せ方

- 週次レポート
- 今週伸びた分野
- 次に伸ばす分野
- 試験日までの必要ペース変化
- 「準備度」は合格確率ではないことを維持
- 公開問題・模試の成績を使った実試験基準との比較表示は慎重に設計

### v345 — Premium / 課金パイロット

課金前に無料βユーザーで価値検証する。

有料価値の中心候補:

- クラウド同期 / 複数端末
- 高度な自動学習計画
- 詳細な弱点分析
- 模試・直前期プラン
- 高度な処方ドリル

**問題数だけを主な課金壁にしない。** FE QUESTの商品価値は「あなた専用の合格計画」に置く。

期間課金（例: 90日プラン）を月額サブスクより優先的に検討する。

### v346以降 — 外部β / 一般公開

- 10〜30人程度の外部βから開始
- 1日目、3日目、7日目、30日目の継続率を観察
- 自動計画の利用率
- 「今日やることを自分で考えずに済んだか」を価値指標として調査
- 問題・教材の著作権監査を公開前に再実施

---

## 6. 現時点で後回しにするもの

以下は面白いが、v342以前の優先事項ではない。

- 睡眠時間・体調をユーザーに毎日入力させる機能
- 大規模なバッジ / SNS / ランキング
- AIチャットの高度化だけを目的とした大型機能
- 合格確率を断定する予測モデル
- 対応資格の大量追加

疲労などを扱う場合は、まず回答時間・連続誤答・時間帯別成績など**既に自然に取得できるデータ**から推定する方向を優先する。

---

## 7. UX監査の継続ルール

現在進めている「毎日使ったときに気になる余白・情報量・操作導線・スマホ表示」の全画面監査は継続する。

優先順:

1. ホーム
2. 学習計画
3. 教材
4. 科目A問題演習 / 今日の復習
5. 結果画面
6. 科目B
7. 模試
8. 学習分析
9. アプリ・データ / 復旧センター

共通基準:

- 1画面1主目的
- 最初の1画面で「現在地」と「次の1手」が分かる
- 日常的に不要な詳細は折りたたむ
- 主CTA位置をできるだけ統一
- 次画面 / 次問題では読み始め位置を自然にする
- iPhoneのsafe-area、固定ヘッダー、固定下部ナビを実機基準で確認
- UI修正のためだけに無制限にversioned overrideを増やさない

---

## 8. 商用化のゲート

### 無料外部βに進める条件

- v340までの初回UX改善
- 重大なデータ消失経路なし
- 教材・問題の著作権再監査
- 利用規約 / プライバシー方針の草案

### 有料βに進める条件

原則として v342 のクラウド同期を完了してから。

最低条件:

- アカウント
- クラウドバックアップ / 同期
- 端末変更復旧
- データ削除
- 障害時のローカル継続
- 決済権限をサーバー側で検証可能な構造

### 課金コンセプト

**FE QUEST Premium — あなた専用の合格計画**

問題集を売るのではなく、進捗・弱点・忘却・実績ペース・試験日から、毎日の学習を調整してくれる価値に課金する。

---

## 9. 別チャットでの再開手順

ユーザーが別チャットで「FE QUESTの続きを開発したい」「GitHubの最新版から続けて」と言った場合、担当AIは次の順で確認する。

1. GitHub `taiwanwan64/fe-quest` の `main` を確認
2. `FE_QUEST_DEVELOPMENT_PLAN.md` を読む
3. `index.html` / `manifest.webmanifest` / `sw.js` から現行アプリバージョンを確認
4. 直近のmerged PRを確認し、本ファイル記載の「現行アプリ」が古ければGitHub実体を優先
5. 本ファイルの「次の計画バージョン」と、実際に完了済みのPRを突き合わせる
6. 完了済み項目にチェックを付ける / 文書を更新してから次の開発に進む

### 重要

- 古い会話メモに v107 / v111 / v332 等が残っていても、GitHub `main` とこのファイルを優先する。
- 各主要バージョンを完了したら、**同じ開発セッション内でこのファイルも更新する。**
- 少なくとも「現行アプリ」「次の計画バージョン」「完了した項目」「新たに判明した重大課題」を更新する。
- ロードマップ変更が必要な場合は、理由をこのファイルに残す。

---

## 10. 直近の次アクション

**次に着手する正式タスクは v338「技術的負債・実行経路の全体監査」。**

v338では機能追加を急がず、以下を最初に行う。

1. runtime hard assert 全件抽出
2. 多重ラップ関数の抽出と深度計測
3. versioned override の責務・依存関係マップ作成
4. 完成版3.67MBの構成比分析
5. 自動調整ロジックの回帰契約強化
6. v339で安全に除去できる対象の確定

この監査中でも、ユーザーが実使用で発見した明確なUX不具合は優先的に修正してよい。ただし、ロードマップの主目的を見失わない。
