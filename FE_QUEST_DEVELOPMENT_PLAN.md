# FE QUEST 開発計画 / 引き継ぎドキュメント

> **このファイルは FE QUEST 開発の Source of Truth（引き継ぎ元）です。**
>
> 別チャット・別AIで開発を再開するときは、最初に GitHub の `main`、開いているPR / Issue、Actions、そして本ファイルを確認してください。会話履歴や古い記憶より、GitHub の現行コードと本ファイルを優先します。
>
> v343までの詳細な旧計画は `docs/archive/FE_QUEST_DEVELOPMENT_PLAN_through_v343.md` に原文のまま保存しています。

最終更新: **2026-09-02**
現行の学習者向け本番アプリ: **v358**
プロフィールスキーマ: **v5**  
最新の開発・受入マイルストーン: **v358（論理回路／オートマトンの入力追跡図）**
現在のフェーズ: **10〜30人の小規模外部β**  
現在の運用Issue: **#143 External beta cohort 1**  
リポジトリ: `taiwanwan64/fe-quest`

---

## 1. プロダクトの北極星

FE QUEST は、基本情報技術者試験の単なる問題集ではなく、**学習者の実際の状況から「今日やること」を自動調整し、合格までの学習運転を担う適応型学習アプリ**として育てる。

中心メッセージは次の通り。

> **「今日やることを自分で考えなくても、FE QUESTを開けば合格に近づく学習が始められる」**

教材 → 科目A演習 → 復習 → 弱点補強 → 科目B → 模試 → 学習計画 → 直前期調整、という一連の接続を壊さないことを最優先にする。

既存の適応学習は、試験日、1日の学習時間、直近の学習ペース、問題ごとの正誤・所要時間、忘却状態、誤答理由、教材進捗、科目B進捗、模試履歴などを使って日々の学習内容を調整している。リファクタリングや新機能追加で、この挙動や保存済み学習データを不用意に壊してはいけない。

---

## 2. バージョンの読み方

**本番アプリのバージョンと、開発・監査マイルストーンの番号を混同しない。**

- 本番の学習者向けアセットは現在 **v358**。
- v346〜v350 は、外部βの準備・監査・受入・引き継ぎ・運用開始を表す開発マイルストーンであり、学習者向け本番アセットを進めていない。
- v351は、実際のデスクトップ確認で見つかった「進捗・設定」の表示摩擦だけを直した学習者向けリリース。
- v351はカード内の読みやすさを修正し、v352はカード同士の優先順位・比較・左右バランスを修正した。
- v353は「負の2進数」の仕組みを本文内で確認できる静的図解を追加した。
- v354は「データベース設計」の正規化前後を比較できる静的図解を追加した。
- v355は公開鍵暗号とデジタル署名で使う鍵を上下比較できる静的図解を追加した。
- v356はサブネットマスクのネットワーク部とホスト部の境界を3段で追える静的図解を追加した。
- v357はレジスタから補助記憶までの速度・容量の一般的傾向を階層で比較できる静的図解を追加した。
- v358は論理回路の中間出力とオートマトンの現在状態を入力ごとに追える静的図解を追加した。
- v358の既存アセットを同じ名前のまま直接書き換えない。
- 次に学習者向け挙動を変更する場合は、意図した新しい本番バージョンとして既存の分割リリース手順に沿って公開する。

この区別を曖昧にすると、Service Worker、キャッシュ、復旧、ロールバック、検証結果の対応関係が崩れるため、必ず明示する。

---

## 3. 現在の本番ベースライン

### Production

- `index.html` → `app/base-shell-v358.html`
- `sw.js` → `APP_VERSION = 'v358'`
- `assets/app-v358.js`
- profile schema: **5**
- 問題数: **710問**

### 保存・復旧

既存の安全設計を維持する。

- 原子的保存 / 保存失敗時ロールバック
- 複数タブ競合防止
- migration journal
- 更新前・更新後の復旧点
- Recovery Center
- JSONエクスポート / 復元
- 永続ストレージ関連UI
- 保存領域逼迫時の保護
- 復帰時再検証

本番の学習データを壊さないことを、機能追加より優先する。

### クラウド同期

v342で追加した Supabase 同期は **任意・local-first** のまま維持する。

- ログインしなくても学習できる
- Magic Link + PKCE
- RLS / CAS
- 競合時は勝手に上書きせず、どちらを残すか明示的に選ぶ
- ログアウトしてもローカル学習データは維持
- アカウント削除導線を保持
- optional cloud asset/runtime failure はローカル学習を妨げない fail-open を維持

---

## 4. v340以降の完了済みマイルストーン

### v340 — 初回導線・学習開始UX

初回に受験予定日と学習時間を設定し、今日の計画を自動作成。実力診断から学習開始へ接続し、「今日の学習」を主動線に統一した。

### v341 — 保存・復旧契約の強化

保存失敗、更新、複数タブ、復旧点、Recovery Centerを含む安全契約を強化した。

### v342 — 任意クラウド同期

local-first を維持したまま Supabase 同期を追加。競合の明示解決、アカウント削除、JSON / Recovery Centerとの共存を実装した。

### v343 — 適応判定の精度向上

学習者の履歴に応じた判断を細分化し、推定の過信を避けながら既存履歴を利用する設計を強化した。

### v344 — 最近の学習成果

直近の保存済み回答を有限の窓で比較し、「最近伸びた分野」「次に伸ばすポイント」を提示するようにした。

### v345 — 試験日連動ペース表示

既存の `examPaceStatus()` を再利用し、受験予定日と進捗から学習ペースを表示。**合格確率ではない**ことを明示し、直前期テーパリングを優先した。

### v346 — 外部β準備

- `privacy.html` を現行v345へ同期
- βフィードバックIssueテンプレートを追加
- 10〜30人を想定した測定計画を作成
- 第三者の行動分析SDK / サイレント計測を追加しない方針を明文化

参照: `docs/BETA_MEASUREMENT_PLAN_v346.md`

### v347 — β学習者ジャーニードライラン

初回設定 → 実力診断 → 今日の計画 → 日次継続 → 学習成果 → 任意クラウド → フィードバックを監査。

- 51 / 51 PASS
- fresh runtime の今日タスク: review / lesson / subjectB / boss の4件
- Current Contract: 71 / 71
- Browser UI: 23 / 23
- runtime failure: 0

参照: `docs/EXTERNAL_BETA_DRY_RUN_v347.md`, `audits/V347_EXTERNAL_BETA_JOURNEY_DRY_RUN.md`

### v348 — 本番ブラウザ自動受入

GitHub Pages本番を desktop Chromium と mobile-sized Playwright WebKit で検証。

- Chromium first-run lifecycle: 6 / 6 PASS
- optional cloud fail-open: PASS
- 初回設定、4タスク生成、学習開始、診断1問目、再読込後の設定保持、privacyを確認
- uncaught page errors: 0
- automated production browser gate: **CLEARED**

参照: `audits/V348_PRODUCTION_BROWSER_ACCEPTANCE.md`

> Playwright WebKit のPASSだけを「実機iPhone Safari確認済み」と表現してはいけない。

### v349 — Source of Truth 同期 / β運用引き継ぎ

- 古いv343→v344計画を現行production v345へ同期
- 本番アプリ版とβ準備マイルストーンを明確に分離
- obsoleteだった「v345 = Premium / Paywall Pilot」を廃止
- 旧計画をarchiveへ保存
- 物理デバイス受入と外部β運用の手順を整備

参照: `docs/PHYSICAL_DEVICE_BETA_GATE.md`, `docs/EXTERNAL_BETA_ROLLOUT_PLAYBOOK.md`, `docs/BETA_FEEDBACK_LOG_TEMPLATE.md`

### v350 — 物理デバイスβゲートPASS / Cohort 1準備

2026-08-25、運営者から実機チェック **全項目PASS** の報告を受領した。

- Issue #142 の12チェックを PASS として記録し完了
- 起動不能 / 保存不能 / データ消失 blocker の報告なし
- 端末名 / OS / ブラウザ詳細は報告されていないため、後から推測して記録しない
- 10〜30人の初期外部βを追跡する Issue #143 を作成

これにより、**物理デバイスを含むβ前Go/No-Goゲートはクリア済み**とする。

### v351 — 進捗・設定のデスクトップ整形

実際のデスクトップ確認で見つかった、右カラムの押し潰れと不自然な改行を修正した。

- 十分な画面幅では計画画面の本文幅と右カラム幅を拡大
- 中間幅では無理な2カラムをやめ、読みやすい1カラムへ切り替え
- 「アカウント・クラウド同期」の見出し・説明・状態表示を自然な折り返しへ整理
- メールアドレス入力欄をカード幅いっぱいに拡大し、送信ボタンを独立配置
- 「アプリ・データ」のPWA案内をアイコン・説明・操作の安定したGridへ変更
- 状態タイルを利用可能幅に応じて1 / 2 / 3列で切り替え
- profile schema v5、学習内容、適応ロジック、保存・復旧、v342クラウドruntimeは変更なし

参照: `audits/V351_PROGRESS_SETTINGS_LAYOUT.md`

### v352 — 学習判断優先の進捗ダッシュボード再編

進捗・設定の詳細を、旧来の左メイン / 右設定レールではなく、学習者が判断する順番へ再編した。

- 「試験日ペース」と「今日の配分」を最上段へ配置
- 同じ7日グラフである「今週の学習」と「7日間の復習予報」を隣接比較
- 下段は「試験準備度＋学習設定」と「記憶の健康状態＋学習ロードマップ」の独立した均等幅列にし、左右の総高さも均衡化
- 詳細な仕上げ判定を折り畳み、主要な準備度を先に表示
- クラウド同期とアプリ・データは学習カードより後ろの補助折り畳みへ移動
- profile schema v5、問題・教材、適応ロジック、保存・復旧、クラウド同期挙動は変更なし

参照: `audits/V352_PROGRESS_DASHBOARD.md`

### v353 — 負の2進数レッスン内図解

「負の2進数」の文章説明に、8bitの `+5` から `-5` の2の補数表現を作る3ステップ図解を追加した。

- `+5  0000 0101` と `-5  1111 1011` は同じ部品構造で、符号とbit列の間では折り返さない
- PCでは横方向に流れを比較し、スマートフォンではステップ単位で縦に並べる
- 静的な理解補助として表示し、学習完了のためのクリック操作は増やさない
- 他テーマへの一括展開はせず、まず1レッスンで有効性と読みやすさを確認する
- profile schema v5、問題、学習進捗、適応ロジック、保存・復旧、クラウド同期は変更なし

参照: `audits/V353_INLINE_LESSON_DIAGRAM.md`

### v354 — データベース正規化の前後比較図

「データベース設計」の文章例に、重複を含む受注明細が役割別の3表へ分かれる図解を追加した。

- 正規化前の1表で、注文情報と商品情報の重複を色分け
- 正規化後は「注文」「商品」「注文明細」を同じデータで比較
- 注文ID・商品IDが表を結ぶ鍵であることを表示
- PCでは前後を左右比較し、スマートフォンでは前→後の順で縦に表示
- profile schema v5、問題、既存教材本文、学習進捗、適応ロジック、保存・復旧、クラウド同期は変更なし

参照: `audits/V354_DATABASE_NORMALIZATION_DIAGRAM.md`

### v355 — 公開鍵暗号／デジタル署名の鍵比較図

「暗号技術の基本」と「デジタル署名と認証局」に、鍵の使い方を上下で比較する図解を追加した。

- 公開鍵暗号は「受信者の公開鍵で暗号化 → 受信者の秘密鍵で復号」
- デジタル署名は「文書のハッシュを署名者の秘密鍵で署名 → 署名者の公開鍵で検証」
- 同じ列位置と共通の鍵色で、鍵の向きが逆になることを比較
- PCでは2段×5列、スマートフォンでは各段を縦方向へ配置
- profile schema v5、問題、既存教材本文、学習進捗、適応ロジック、保存・復旧、クラウド同期は変更なし

参照: `audits/V355_CRYPTO_SIGNATURE_KEY_COMPARISON.md`

### v356 — サブネットのネットワーク／ホスト境界図

「サブネットマスク」に、`192.168.1.130/26`のネットワーク部とホスト部を同じ位置で追う図解を追加した。

- `/26`をネットワーク部26bitとホスト部6bitへ色分け
- IPアドレス、サブネットマスク、ネットワークアドレスの境界位置を統一
- `255.255.255.192`の2進構造を表示
- ホスト部を全0にして`192.168.1.128`を得る流れを表示
- profile schema v5、問題、既存教材本文、学習進捗、適応ロジック、保存・復旧、クラウド同期は変更なし

参照: `audits/V356_SUBNET_BOUNDARY_DIAGRAM.md`

### v357 — 速度・容量によるメモリ階層図

「さまざまな記憶装置」に、CPUからの近さと速度・容量の一般的傾向を同時に追う図解を追加した。

- 上からレジスタ、キャッシュ、主記憶、補助記憶の順に配置
- 下へ行くほど階層の幅を広げ、容量が大きくなる傾向を表現
- PCでは速度と容量の縦軸を左右に表示
- スマートフォンでは「上ほど高速・小容量」「下ほど低速・大容量」をコンパクトに表示
- SSDとHDDの個別性能を断定せず、補助記憶としての役割を説明
- profile schema v5、問題、既存教材本文、学習進捗、適応ロジック、保存・復旧、クラウド同期は変更なし

参照: `audits/V357_MEMORY_HIERARCHY_DIAGRAM.md`

### v358 — 論理回路／オートマトンの入力追跡図

「論理回路」と「オートマトン」に、入力を一段ずつ適用して中間値・現在状態を更新する図解を追加した。

- 論理回路は`A=1, B=0`からOR・NOTの中間値を求め、最後にANDへ渡す
- `(A OR B) AND (NOT B)`を3段階で計算し、出力1まで追跡
- オートマトンは入力列`1 → 0 → 1`を状態Aから一文字ずつ適用
- 状態遷移を`A → B → B → A`として表示し、入力0では現在状態を維持することを明示
- PCでは横方向、スマートフォンでは縦方向に追跡
- profile schema v5、問題、既存教材本文、学習進捗、適応ロジック、保存・復旧、クラウド同期は変更なし

参照: `audits/V358_LOGIC_AUTOMATA_TRACE_DIAGRAMS.md`

---

## 5. 現在の外部βフェーズ

### Go / No-Go

**GO。**

以下を完了している。

- privacy / feedback / measurement準備
- コード / runtime の学習者ジャーニー監査
- 本番Chromium / WebKit自動受入
- 任意クラウド fail-open確認
- 物理デバイスでの実機受入: **12 / 12 PASS（Issue #142）**

### Cohort 1

運用トラッカー: **Issue #143**

最初は数人の初回利用を確認し、P0 / P1 blocker がなければ合計 **10〜30人**まで広げる。

この段階で最優先に確認するのは次の2点。

1. 「今日の学習」から実際に学習を開始できたか
2. **次に何を勉強するかを自分で考える負担が減ったか**

D1 / D3 / D7 / D30 は **yes / no / unconfirmed** を中心に手動で追う。アプリから学習履歴を自動送信して判定しない。

### β中の基本姿勢

- 新機能を先回りして増やさない
- P0 / P1 は新規招待を止めて修正
- P2 / P3 は記録し、中心価値への影響順に整理
- 実利用で最も大きい摩擦を小さく修正する
- βフィードバックなしに大規模問題追加やUI全面改修へ進まない

---

## 6. 現在のガードレール

次を、明示的な設計変更なしに破らない。

- 問題数・問題内容・教材内容を不用意に変更しない
- profile schemaを不要に上げない
- 学習履歴を破壊 / 取りこぼししない
- local-firstを維持
- クラウド競合を黙って上書きしない
- Recovery Center / JSON復元を後退させない
- 試験準備度・学習ペースを「合格確率」と表現しない
- 第三者の行動分析SDKやサイレントトラッキングを勝手に追加しない
- 小規模βの中心価値を確認する前にPremium / Paywallを実装しない
- 有料サービスを勝手に導入しない
- 公開Issueにメールアドレス、Magic Link、認証トークン、フルJSON、localStorage / IndexedDB全量などを貼らせない
- βテスターの実名・メールアドレス等を公開リポジトリへ記録しない

---

## 7. 問題・教材の方針

参考書・問題集は、**学習構成、説明粒度、出題傾向、難易度設計の参考**として使う。

- 実際の基本情報技術者試験で公開された問題を除き、参考書・問題集の独自問題をそのまま転載しない
- アプリ内問題は原則オリジナル
- 略語は初出で正式名称を併記
- ITパスポート既習者でも忘れている前提で、基礎から理解できる説明にする
- 「必ず・常に・だけ」「行わない・後回し」など、正解を不自然に透かす選択肢を増やさない
- 科目Bは途中状態予測や実戦に近い思考を重視し、単なる暗記問題化を避ける

新規問題の大量追加より、βで実際に詰まった箇所を優先して改善する。

---

## 8. 次の優先順位

### Priority 1 — 最初の数人を開始

Issue #143 に沿って、公開GitHubには個人情報を残さず、最初の数人の初回利用を確認する。

### Priority 2 — 10〜30人へ拡大

P0 / P1 がないことを確認したら、Cohort 1 を10〜30人へ広げる。

### Priority 3 — D1 / D3 / D7 の実利用から最大の摩擦を特定

候補を先に決めつけず、実際の回答・不具合から優先度を決める。特に見る候補は以下。

- 初回設定
- 「今日の学習」開始までの迷い
- プライバシー / フィードバック導線の発見しやすさ
- 科目Bの次行動
- 学習成果 / ペース表示の理解
- 保存 / 復旧 / 任意クラウドへの安心感

### Priority 4 — 観測された摩擦だけを小さく修正

学習者向けコードを変更する場合は新しいversioned releaseを使う。現行v358を同名のまま書き換えない。

---

## 9. 次の学習者向けリリース規則

学習者向けコードを変更するときは、現在のv358アセットを同名のまま上書きしない。

原則:

1. `main` の最新状態を確認
2. βで観測された具体的な問題を特定
3. 変更の目的と安全境界を決める
4. feature branchで実装
5. 既存contract / regression / browser testを通す
6. 新しいversioned split assetを生成
7. staging / release validation
8. release PR
9. mainへマージ後、GitHub Pages本番を確認
10. rollback可能性と学習データ非破壊を再確認

機能を増やすことより、**本番データを安全に保ったまま、小さな差分を検証可能な形で出すこと**を優先する。

---

## 10. マネタイズについて

旧計画のPremium / Paywall Pilotは現在の既定路線ではない。

再検討の条件:

- 小規模外部βで継続利用の兆候が確認できる
- 「今日やることを考えずに済む」という中心価値が支持される
- 保存 / 復旧 / プライバシーへの重大な不安がない
- 何を無料、何を有料にするかが学習価値を損なわず説明できる

それまではPaywall実装を先行しない。

---

## 11. 再開時チェックリスト

別チャット・別AI・別セッションから再開するときは、次の順で確認する。

1. GitHub `main` の最新コミット
2. 開いているPR / Issue（特にCohort 1の #143）
3. GitHub Actions の直近結果
4. 本 `FE_QUEST_DEVELOPMENT_PLAN.md`
5. 現行productionの `index.html`, `sw.js`, versioned app assets
6. 作業対象に関係するaudit / fixture / workflow

そのうえで、古い会話記憶ではなく**現行GitHubを基準に**問題特定 → 修正 → 検証 → PR → マージまで進める。

---

## 12. 現在地点の一文要約

> **FE QUESTは、本番v358 / schema v5として、β前ゲートを維持したまま「負の2進数」「データベース設計」「公開鍵暗号／デジタル署名」「サブネットマスク」「記憶装置」「論理回路／オートマトン」に本文内図解を追加した。現在はIssue #143で10〜30人の小規模外部βを進め、次の開発判断も実利用のD1 / D3 / D7データと具体的な摩擦を根拠に行う。**
