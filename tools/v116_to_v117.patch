--- a/index.html
+++ b/index.html
@@ -8,7 +8,7 @@
 <meta name="apple-mobile-web-app-title" content="FE QUEST" />
 <meta name="description" content="基本情報技術者試験の学習・問題演習・復習・模試をまとめた学習PWA" />
 <meta name="apple-mobile-web-app-status-bar-style" content="default" />
-<title>FE QUEST PWA v116</title>
+<title>FE QUEST PWA v117</title>
 <style>
   :root{
     --bg:#f6f8fb;
@@ -6498,7 +6498,7 @@
 const RECOVERY_CHECKPOINT_INTERVAL = 30*60*1000;
 const WRITER_LEASE_MS = 12000;
 const PROFILE_SCHEMA_VERSION = 5;
-const APP_VERSION = 'v116';
+const APP_VERSION = 'v117';
 const TAB_INSTANCE_ID = (()=>{try{return crypto.randomUUID()}catch(_e){return `tab-${Date.now()}-${Math.random().toString(36).slice(2)}`}})();
 let profileRecoveryWarning = false;
 let profileRecoverySource = '';
@@ -6515,6 +6515,10 @@
 let profileCommittedSnapshot = null;
 let lastProfileSaveFailure = '';
 let lastProfileSaveFailureAt = 0;
+// v117: the script is intentionally one file, so several modules backfill profile fields
+// before later modules have declared their datasets.  Never persist+render during that phase.
+let appBootComplete = false;
+let bootProfileSavePending = false;
 
 const DEFAULT_PROFILE = {
   profileSchemaVersion: PROFILE_SCHEMA_VERSION,
@@ -6974,29 +6978,39 @@
 rememberCommittedProfile(profile);
 
 function saveProfile(){
+  // v117: top-level schema/backfill calls made while the one-file bundle is still
+  // declaring later modules must never refresh the whole UI. Defer them into one
+  // final save after every dataset/renderer exists.
+  if(!appBootComplete){bootProfileSavePending=true;return true;}
   if(profileWriteBlocked||profileConflictBlocked){restoreCommittedProfileInMemory(true);return false;}
   if(!acquireProfileWriteLease()){
     restoreCommittedProfileInMemory(true);
     markProfileConflict('別のFE QUEST画面がいま学習データを書き込んでいます');return false;
   }
+  let saved=false;
   try{
     profile=stampProfileForSave(profile);
     const result=writeCurrentProfile(profile,{preservePrevious:true});
     profile=result.profile;rememberCommittedProfile(profile);clearProfileSaveFailure();
-    queueRecoveryCheckpoint('autosave',false);
-    refreshProfileUI();
-    return true;
+    saved=true;
   }catch(e){
-    restoreCommittedProfileInMemory(true);noteProfileSaveFailure(e);
+    restoreCommittedProfileInMemory(false);noteProfileSaveFailure(e);
     if(e?.code==='PROFILE_REVISION_CONFLICT'){markProfileConflict();return false}
     profileWriteBlocked=true;
     console.warn('学習データの保存に失敗しました。',e);
-    setTimeout(()=>showAppNotice('error','保存を停止しました','未保存の変更は最後に正常保存できた状態へ戻しました。保存領域不足やブラウザ制限の可能性があります。学習を続ける前に、まず学習データを書き出すか再読み込みしてください。','設定を開く',()=>{
-      showScreen('plan');setTimeout(()=>document.getElementById('pwaHealthCard')?.scrollIntoView?.({behavior:'smooth',block:'center'}),80);
-    }),0);
-    refreshPwaHealth?.();
+    setTimeout(()=>showAppNotice('error','保存できませんでした','現在の学習データは最後に正常保存できた状態へ戻しました。ページを再読み込みすると改善する場合があります。','再読み込み',()=>location.reload()),0);
+    setTimeout(()=>refreshPwaHealth?.(),0);
     return false;
   }finally{releaseProfileWriteLease()}
+
+  // A recovery-checkpoint or UI-rendering bug must never be misclassified as a
+  // storage failure. Persistence has already succeeded at this point.
+  try{queueRecoveryCheckpoint('autosave',false)}catch(e){console.warn('Recovery checkpoint queue failed',e)}
+  try{refreshProfileUI()}catch(e){
+    console.warn('UI refresh failed after a successful save',e);
+    setTimeout(()=>reportGlobalError?.(),0);
+  }
+  return saved;
 }
 
 function clamp(n,min,max){ return Math.max(min, Math.min(max,n)); }
@@ -17249,22 +17263,26 @@
   return t.type;
 }
 function persistProfileSilently(){
+  if(!appBootComplete){bootProfileSavePending=true;return true;}
   if(profileWriteBlocked||profileConflictBlocked){restoreCommittedProfileInMemory(false);return false;}
   try{if(profileCommittedChecksum&&profileIntegrityChecksum(profile)===profileCommittedChecksum)return true}catch(_e){}
   if(!acquireProfileWriteLease()){restoreCommittedProfileInMemory(false);return false;}
+  let saved=false;
   try{
     profile=stampProfileForSave(profile);
     const result=writeCurrentProfile(profile,{preservePrevious:true});
     profile=result.profile;rememberCommittedProfile(profile);clearProfileSaveFailure();
-    queueRecoveryCheckpoint('autosave',false);
-    return true;
+    saved=true;
   }catch(e){
     restoreCommittedProfileInMemory(false);noteProfileSaveFailure(e);
     if(e?.code==='PROFILE_REVISION_CONFLICT')markProfileConflict();
-    else{profileWriteBlocked=true;console.warn('silent save failed',e);refreshPwaHealth?.();}
+    else{profileWriteBlocked=true;console.warn('silent save failed',e);setTimeout(()=>refreshPwaHealth?.(),0);}
     return false;
   }finally{releaseProfileWriteLease()}
+  try{queueRecoveryCheckpoint('autosave',false)}catch(e){console.warn('Recovery checkpoint queue failed',e)}
+  return saved;
 }
+
 function snapshotDailyTasks(tasks){
   return (tasks||[]).map(t=>structuredClone({...t,slot:dailyTaskSlot(t)}));
 }
@@ -18439,12 +18457,8 @@
   setPlanDetailsOpen(document.getElementById('plan')?.classList.contains('plan-compact'));
 });
 
-// v115: first safe full UI initialization.  CORE_A_IDS, Subject B datasets, and
-// the daily-plan engine are all defined by this point, so CTA handlers can be attached safely.
-window.FEQUEST_DAILY_PLAN_READY=true;
-refreshProfileUI();
-openProblemsHub();
-renderLearningEntry();
+// v117: full UI initialization is deferred until the end of the bundle.
+// Later modules (weakness/Rx/PWA) still declare dependencies after this point.
 
 
 // ===== Exam area coverage =====
@@ -19955,10 +19969,7 @@
 
 // From this line onward, HOME may safely use the Rx engine.
 window.FEQUEST_RX_READY=true;
-refreshPrescriptionCard();
-renderWeakDashboard();
-renderDailyPlan();
-renderPlannerScreen();
+// v117: initial rendering still waits for the final bundle boot barrier.
 
 const _refreshProfileUIV16=refreshProfileUI;
 refreshProfileUI=function(){
@@ -20482,7 +20493,7 @@
     const backupSelf=makeBackupPayload(profile),decodedSelf=decodeBackupPayload(backupSelf);
     assert(decodedSelf.profile.profileSchemaVersion===PROFILE_SCHEMA_VERSION,'Backup roundtrip schema');
     assert(profileIntegrityChecksum(decodedSelf.profile)===backupSelf.checksum,'Backup checksum roundtrip');
-    assert(PROFILE_SCHEMA_VERSION===5&&APP_VERSION==='v116','v116 version/schema contract drift');
+    assert(PROFILE_SCHEMA_VERSION===5&&APP_VERSION==='v117','v116 version/schema contract drift');
     assert(typeof renderPlanFocus==='function'&&!!document.getElementById('planFocusCard')&&!!document.getElementById('planDetailsToggle'),'v111 plan next-action / compact details missing');
     assert(!!document.querySelector('#history .analytics-priority-card')&&!!document.getElementById('analyticsDetailsToggle'),'v111 analytics priority / progressive disclosure missing');
     assert(!!document.querySelector('#pwaHealthCard .data-maintenance-fold')&&!!document.querySelector('#pwaHealthCard .recovery-fold'),'v111 data/recovery progressive disclosure missing');
@@ -20504,7 +20515,7 @@
     assert(!!document.querySelector('#weak > .weak-detail-fold')&&document.querySelector('#weak > .weak-detail-fold')?.contains(document.getElementById('weakCategoryGrid')),'v114 weak-analysis details must be secondary');
     assert(!!document.getElementById('weakTopAction'),'v114 weak-analysis priority must have a direct action');
     assert(document.querySelector('#coverage .coverage-summary-compact')?.textContent.includes('教材の総合進捗'),'v114 coverage duplicate summary reduction missing');
-    assert(window.FEQUEST_DAILY_PLAN_READY===true&&typeof CORE_A_IDS!=='undefined'&&CORE_A_IDS.length===130,'v115 daily-plan UI must initialize only after Core A data is ready');
+    assert(typeof CORE_A_IDS!=='undefined'&&CORE_A_IDS.length===130&&typeof finalizeAppBoot==='function'&&String(finalizeAppBoot).includes('FEQUEST_DAILY_PLAN_READY=true'),'v115/v117 daily-plan boot barrier missing');
     assert(document.querySelector('header #aiFab.ai-header-btn'),'v115 BIT先生 must live in the top-right header');
     assert(typeof subjectBHubRecommendation==='function'&&typeof renderSubjectBHub==='function'&&typeof launchSubjectBRecommendation==='function','v110 Subject B guided entry missing');
     assert(!!document.getElementById('subjectBNextCard')&&!!document.getElementById('subjectBProgressStrip'),'v110 Subject B hub UI missing');
@@ -20533,6 +20544,8 @@
     assert(typeof revalidateProfileFreshness==='function'&&String(revalidateProfileFreshness).includes('profileBaseRevision'),'v99 resume freshness guard missing');
     assert(typeof applyPendingUpdateSafely==='function'&&String(applyPendingUpdateSafely).includes('pre-update'),'v99 safe-update checkpoint missing');
     assert(typeof activatePendingUpdateOrReload==='function'&&String(activatePendingUpdateOrReload).includes('SKIP_WAITING')&&String(applyPendingUpdateSafely).includes('profileConflictBlocked||profileWriteBlocked'),'v116 one-tap safe-update path missing');
+    assert(typeof appBootComplete==='boolean'&&typeof finalizeAppBoot==='function'&&String(finalizeAppBoot).includes('appBootComplete=true')&&String(finalizeAppBoot).includes('FEQUEST_APP_BOOT_COMPLETE=true'),'v117 final bundle boot barrier missing');
+    assert(String(saveProfile).includes('if(!appBootComplete)')&&String(persistProfileSilently).includes('if(!appBootComplete)'),'v117 boot-time save deferral missing');
     assert(String(restorePreImportProfile).includes('localStorage.removeItem(PROFILE_ATOMIC_KEY)'),'v99 future-schema pre-import rollback must clear atomic authority');
     assert(String(exportLearningData).includes("profileRecoverySource==='future'"),'v99 must not export empty fallback profile over future-schema data');
     assert(typeof verifyProfileDurability==='function'&&typeof createRecoveryPointNow==='function'&&typeof ensureVersionRecoveryCheckpoint==='function','v99 recovery operations missing');
@@ -20728,7 +20741,11 @@
   appNoticeTitle.textContent=title;
   appNoticeBody.textContent=body;
   if(actionLabel&&action){
-    appNoticeAction.style.display='';appNoticeAction.textContent=actionLabel;appNoticeAction.onclick=action;
+    appNoticeAction.style.display='';appNoticeAction.textContent=actionLabel;
+    appNoticeAction.onclick=()=>{
+      hideAppNotice();
+      try{action()}catch(e){console.warn('Notice action failed',e);setTimeout(()=>reportGlobalError?.(),0)}
+    };
   }else{
     appNoticeAction.style.display='none';appNoticeAction.onclick=null;
   }
@@ -21434,6 +21451,31 @@
     setTimeout(()=>showAppNotice('error','学習データを読み取れませんでした','正常なローカルスナップショットや長期復旧点も利用できなかったため、新規状態で起動しました。破損した元データは復旧用として端末内に退避しています。','設定を開く',()=>{showScreen('plan');setTimeout(()=>document.getElementById('pwaHealthCard')?.scrollIntoView?.({behavior:'smooth',block:'center'}),80)}),300);
   }
 }
+function finalizeAppBoot(){
+  if(appBootComplete)return;
+  // At this exact point every const/let dataset and every UI/PWA module in the
+  // one-file bundle has been declared. Only now may a save trigger full rendering.
+  appBootComplete=true;
+  window.FEQUEST_APP_BOOT_COMPLETE=true;
+  window.FEQUEST_DAILY_PLAN_READY=true;
+
+  if(bootProfileSavePending){
+    bootProfileSavePending=false;
+    // Persist all schema/default/backfill changes once, instead of repeatedly while
+    // the bundle is still initializing. saveProfile also performs the first full render.
+    if(saveProfile())return;
+  }
+  try{
+    refreshProfileUI();
+    openProblemsHub();
+    renderLearningEntry();
+  }catch(e){
+    console.warn('Final app boot render failed',e);
+    setTimeout(()=>reportGlobalError?.(),0);
+  }
+}
+finalizeAppBoot();
+
 finalizeProfileRecovery().catch(e=>console.warn('Profile recovery finalization failed',e));
 refreshPwaHealth();
 setTimeout(()=>renderRecoveryCenter().catch(e=>console.warn('Recovery center init failed',e)),350);
--- a/manifest.webmanifest
+++ b/manifest.webmanifest
@@ -1,8 +1,8 @@
 {
-  "name": "FE QUEST v116",
+  "name": "FE QUEST v117",
   "short_name": "FE QUEST",
-  "description": "基本情報技術者試験向けPWA。v116ではアプリ更新を1タップ化し、保存停止・別画面競合・更新待機時でも手動の再読み込みやバックアップ操作を要求せず、安全な確定済み学習データを保ったまま最新版へ切り替える更新導線へ改善。v115までの主要CTA復旧、BIT先生配置、日常利用UX、問題・教材データ、データ保護、試験直前調整を維持。",
+  "description": "基本情報技術者試験向けPWA。v117では起動途中の保存処理が未初期化モジュールを参照して保存停止扱いになる不具合を修正。全モジュール宣言後に一度だけ初期保存・描画する起動バリアを導入し、UI描画エラーを保存失敗として扱わないよう分離。v116の1タップ更新導線も維持。v115までの主要CTA復旧、BIT先生配置、日常利用UX、問題・教材データ、データ保護、試験直前調整を維持。",
   "start_url": "./",
   "scope": "./",
   "display": "standalone",
--- a/sw.js
+++ b/sw.js
@@ -1,5 +1,5 @@
-const APP_VERSION = 'v116';
-const CACHE_NAME = 'fe-quest-v116-1';
+const APP_VERSION = 'v117';
+const CACHE_NAME = 'fe-quest-v117-1';
 const CACHE_PREFIX = 'fe-quest-';
 const APP_SHELL = [
   './',
@@ -10,6 +10,9 @@
 ];
 
 self.addEventListener('install', event => {
+  // v117 emergency hotfix: v116 can enter a false save-block loop during startup.
+  // Activate this release immediately so the learner only needs one normal reload.
+  self.skipWaiting();
   event.waitUntil(
     caches.open(CACHE_NAME).then(cache =>
       cache.addAll(APP_SHELL.map(url => new Request(url,{cache:'reload'})))
