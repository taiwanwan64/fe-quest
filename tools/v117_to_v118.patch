--- feq_v117_base_extract/index.html	2026-08-14 22:43:12.000000000 +0000
+++ feq_v118_work/index.html	2026-08-14 22:57:07.961770034 +0000
@@ -8,7 +8,7 @@
 <meta name="apple-mobile-web-app-title" content="FE QUEST" />
 <meta name="description" content="基本情報技術者試験の学習・問題演習・復習・模試をまとめた学習PWA" />
 <meta name="apple-mobile-web-app-status-bar-style" content="default" />
-<title>FE QUEST PWA v117</title>
+<title>FE QUEST PWA v118</title>
 <style>
   :root{
     --bg:#f6f8fb;
@@ -1636,6 +1636,8 @@
   }
   .trace-array{
     display:flex;gap:6px;flex-wrap:wrap;justify-content:center;
+    /* v118: reserve a real row for array indices so labels below never overlap them. */
+    padding-bottom:24px;
   }
   .trace-array-cell{
     min-width:46px;height:46px;padding:0 8px;border-radius:12px;
@@ -1645,7 +1647,8 @@
   .trace-array-cell.focus{border-color:var(--blue);background:var(--blue-soft)}
   .trace-array-cell.found{border-color:var(--green);background:var(--green-soft)}
   .trace-array-index{
-    position:absolute;top:48px;font-size:14px;color:var(--muted);
+    position:absolute;top:calc(100% + 4px);left:50%;transform:translateX(-50%);
+    font-size:14px;line-height:1;color:var(--muted);
   }
 
   .trace-stack{
@@ -6498,7 +6501,7 @@
 const RECOVERY_CHECKPOINT_INTERVAL = 30*60*1000;
 const WRITER_LEASE_MS = 12000;
 const PROFILE_SCHEMA_VERSION = 5;
-const APP_VERSION = 'v117';
+const APP_VERSION = 'v118';
 const TAB_INSTANCE_ID = (()=>{try{return crypto.randomUUID()}catch(_e){return `tab-${Date.now()}-${Math.random().toString(36).slice(2)}`}})();
 let profileRecoveryWarning = false;
 let profileRecoverySource = '';
@@ -19986,6 +19989,14 @@
   const attempted=QUESTION_BANK.filter(q=>profile.qStats[q.id]?.attempts>0);
   return attempted.sort((a,b)=>reviewUrgency(b)-reviewUrgency(a)).slice(0,n);
 }
+function reviewTimingLabel(stat){
+  const due=stat?.due;
+  if(!due) return {text:'復習タイミング：自動調整',due:false};
+  const today=localDateISO(0);
+  if(due<=today) return {text:'復習タイミング：今',due:true};
+  const days=Math.max(1,daysBetweenISO(today,due));
+  return {text:days===1?'次回の復習：明日':`次回の復習：${days}日後`,due:false};
+}
 function renderHomeReviewCandidates(){
   const root=document.getElementById('homeReviewCandidates');
   if(!root) return;
@@ -19998,11 +20009,11 @@
   root.innerHTML=items.map(q=>{
     const st=profile.qStats[q.id];
     const r=memoryRetention(st)??100;
-    const risk=Math.max(0,100-r);
+    const timing=reviewTimingLabel(st);
     return `<div class="mini-card"><div class="big">${icons[q.cat]||'🧠'}</div><b>${escapeHtml(q.concept)}</b>
       <div class="sub">${escapeHtml(q.cat)}・${escapeHtml(q.cognitiveLevel||'')}・推定保持 ${r}%</div>
       <div class="progress" style="margin-top:10px"><div style="width:${r}%"></div></div>
-      <div class="memory-risk">${st.due&&st.due<=localDateISO(0)?'復習期限':'忘却リスク'} ${risk}%</div></div>`;
+      <div class="memory-risk" style="${timing.due?'':'color:var(--muted)'}">${escapeHtml(timing.text)}</div></div>`;
   }).join('');
 }
 function renderMemoryHealth(){
@@ -20493,7 +20504,7 @@
     const backupSelf=makeBackupPayload(profile),decodedSelf=decodeBackupPayload(backupSelf);
     assert(decodedSelf.profile.profileSchemaVersion===PROFILE_SCHEMA_VERSION,'Backup roundtrip schema');
     assert(profileIntegrityChecksum(decodedSelf.profile)===backupSelf.checksum,'Backup checksum roundtrip');
-    assert(PROFILE_SCHEMA_VERSION===5&&APP_VERSION==='v117','v116 version/schema contract drift');
+    assert(PROFILE_SCHEMA_VERSION===5&&APP_VERSION==='v118','v118 version/schema contract drift');
     assert(typeof renderPlanFocus==='function'&&!!document.getElementById('planFocusCard')&&!!document.getElementById('planDetailsToggle'),'v111 plan next-action / compact details missing');
     assert(!!document.querySelector('#history .analytics-priority-card')&&!!document.getElementById('analyticsDetailsToggle'),'v111 analytics priority / progressive disclosure missing');
     assert(!!document.querySelector('#pwaHealthCard .data-maintenance-fold')&&!!document.querySelector('#pwaHealthCard .recovery-fold'),'v111 data/recovery progressive disclosure missing');
--- feq_v117_base_extract/manifest.webmanifest	2026-08-14 22:41:52.000000000 +0000
+++ feq_v118_work/manifest.webmanifest	2026-08-14 22:57:07.967616977 +0000
@@ -1,8 +1,8 @@
 {
   "id": "./",
-  "name": "FE QUEST v117",
+  "name": "FE QUEST v118",
   "short_name": "FE QUEST",
-  "description": "基本情報技術者試験向けPWA。v117では起動途中の保存処理が未初期化モジュールを参照して保存停止扱いになる不具合を修正。全モジュール宣言後に一度だけ初期保存・描画する起動バリアを導入し、UI描画エラーを保存失敗として扱わないよう分離。v116の1タップ更新導線も維持。v115までの主要CTA復旧、BIT先生配置、日常利用UX、問題・教材データ、データ保護、試験直前調整を維持。",
+  "description": "基本情報技術者試験向けPWA。v118では科目Bトレースの配列添字とtarget表示の重なりを解消し、復習候補の分かりにくい「復習期限○%」を「復習タイミング：今／次回の復習：○日後」に変更。v117の起動・保存安定化、v116の1タップ更新、主要CTA、BIT先生配置、日常利用UX、問題・教材データを維持。",
   "lang": "ja",
   "start_url": "./",
   "scope": "./",
--- feq_v117_base_extract/sw.js	2026-08-14 22:43:52.000000000 +0000
+++ feq_v118_work/sw.js	2026-08-14 22:57:07.967679244 +0000
@@ -1,5 +1,5 @@
-const APP_VERSION = 'v117';
-const CACHE_NAME = 'fe-quest-v117-1';
+const APP_VERSION = 'v118';
+const CACHE_NAME = 'fe-quest-v118-1';
 const CACHE_PREFIX = 'fe-quest-';
 const APP_SHELL = [
   './',
