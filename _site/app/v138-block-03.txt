// ===== v138 condition-specific distractor explanation pass: データベース =====
const SUBJECT_A_V138_OVERRIDES_3={"db-01":{"choiceExps":["主キーは各行（レコード）を一意に識別するための属性または属性の組です。","主キーは各行（レコード）を一意に識別するための属性または属性の組です。 インデックスは検索位置を効率よく探すための索引で、行を一意に識別する主キーとは役割が異なります。この設問で必要なのは各行を一意に識別するキーです。","「外部キー」は、別の表の主キーなどを参照し、表同士を関連付けるための列です。 この設問では「「この1行は誰？」を一意に決めるキーです」が判断軸で、該当するのは「主キー」です。","主キーは各行（レコード）を一意に識別するための属性または属性の組です。 ビューは問合せ結果を仮想的な表として見せる仕組みで、行を一意に識別するキーではありません。この設問で必要なのは各行を一意に識別するキーです。"]},"db-02":{"choiceExps":["「300000以上」は300000を含み、それより大きい値も対象なので、WHERE salary >= 300000 とします。 <= は300000以下を選ぶため、求める「300000以上」と向きが逆です。","「300000以上」は300000を含み、それより大きい値も対象なので、WHERE salary >= 300000 とします。","「300000以上」は300000を含み、それより大きい値も対象なので、WHERE salary >= 300000 とします。 = は300000ちょうどの行しか選ばず、300001以上の社員を取りこぼします。","「300000以上」は300000を含み、それより大きい値も対象なので、WHERE salary >= 300000 とします。 <> は300000以外を選ぶため、境界値300000を除外し、300000未満も含めてしまいます。"]},"db-03":{"choiceExps":["判断の根拠は「データベース設計では概念モデルから表構造へ落とし込み、正規化で更新時の矛盾や重複を減らします」。このため「検索性能を改善するため、正規化の各段階でインデックス構成を見直す」のように断定すると、問題の条件・定義と一致しない。","正規化は、データの重複を減らし、追加・更新・削除時の不整合（更新異常）を起こしにくくするために行います。 暗号化は機密性を高めるセキュリティ対策です。正規化の目的は表構造を整理して重複や更新異常を減らすことです。","正規化は、データの重複を減らし、追加・更新・削除時の不整合（更新異常）を起こしにくくするために行います。","判断の根拠は「データベース設計では概念モデルから表構造へ落とし込み、正規化で更新時の矛盾や重複を減らします」。このため「更新処理を単純化するため、関連データを一つの表へ集約する」のように断定すると、問題の条件・定義と一致しない。"]},"db-05":{"choiceExps":["注文表の顧客IDのように、別表の主キーなどを参照して表同士の関係を表す属性を外部キーとして設定します。 代替キーは候補キーのうち主キーに選ばれなかったキーで、別表を参照する外部キーとは役割が異なります。この設問で必要なのは別表の主キーなどを参照して表間の関係を表す外部キーです。","注文表の顧客IDのように、別表の主キーなどを参照して表同士の関係を表す属性を外部キーとして設定します。 複合キーは複数列を組み合わせて一意性などを持たせるキーで、別表を参照すること自体を意味しません。この設問で必要なのは別表の主キーなどを参照して表間の関係を表す外部キーです。","注文表の顧客IDのように、別表の主キーなどを参照して表同士の関係を表す属性を外部キーとして設定します。","「外部キー」は別表の主キーなどを参照し、表同士を関連付ける。一方「候補キー」は行を一意に識別できる最小の属性集合。この問題で求めるのは前者である。"]},"db-06":{"choiceExps":["SUMは指定した数値列の値を合計する集約関数です。amount列の合計ならSUM(amount)を使います。 MAXは最大値を1つ求める関数で、全行の金額を足し合わせません。","SUMは指定した数値列の値を合計する集約関数です。amount列の合計ならSUM(amount)を使います。 COUNTはNULLでない値の件数を数える関数で、金額の合計値を求めません。","SUMは指定した数値列の値を合計する集約関数です。amount列の合計ならSUM(amount)を使います。 AVGは平均値を求める関数で、金額の総和ではありません。","SUMは指定した数値列の値を合計する集約関数です。amount列の合計ならSUM(amount)を使います。"]},"db-07":{"choiceExps":["インデックスは検索対象の位置を効率よく探すための索引です。","「トランザクション」は、複数のデータ操作を、まとめて成功または失敗させる処理単位です。 この設問では「本の索引をイメージします」が判断軸で、該当するのは「インデックス」です。","インデックスは検索対象の位置を効率よく探すための索引です。 ビューは問合せ結果を仮想的な表として見せる仕組みで、行を一意に識別するキーではありません。この設問で必要なのは検索位置を効率よく探すためのインデックスです。","「外部キー」は、別の表の主キーなどを参照し、表同士を関連付けるための列です。 この設問では「本の索引をイメージします」が判断軸で、該当するのは「インデックス」です。"]},"db-08":{"choiceExps":["取得する列をSELECTの後ろに列挙し、FROMで表名を指定します。","取得する列をSELECTの後ろに列挙し、FROMで表名を指定します。 SELECTの後ろには取得する列、FROMの後ろには表名を書くため、列と表の位置が逆です。","取得する列をSELECTの後ろに列挙し、FROMで表名を指定します。 * は全列取得を意味するためsalaryなども含んでしまいます。またWHEREは行条件を指定する句で列を列挙する場所ではありません。","取得する列をSELECTの後ろに列挙し、FROMで表名を指定します。 標準SQLの取得構文はGET/INではなくSELECT/FROMを使います。"]},"db-11":{"choiceExps":["集約後のグループに条件を付ける場合はHAVINGを使います。WHEREは集約前の行を絞るための句です。 GROUP BYはどの列ごとにグループ化するかを指定する句で、集計結果を条件で絞る句ではありません。","集約後のグループに条件を付ける場合はHAVINGを使います。WHEREは集約前の行を絞るための句です。 WHEREはグループ化前の各行を絞るため、COUNTのような集約後の結果条件にはHAVINGを使います。","集約後のグループに条件を付ける場合はHAVINGを使います。WHEREは集約前の行を絞るための句です。 ORDER BYは結果の並べ替えを指定する句で、5人以上というグループ条件を除外・採用するための句ではありません。","集約後のグループに条件を付ける場合はHAVINGを使います。WHEREは集約前の行を絞るための句です。"]},"db-13":{"choiceExps":["SQLではNULLとの比較に=を使わず、IS NULLを使います。 SQLの等価演算子は==ではなく=ですが、そもそもNULLは通常の等価比較では判定せずIS NULLを使います。","SQLではNULLとの比較に=を使わず、IS NULLを使います。","SQLではNULLとの比較に=を使わず、IS NULLを使います。 LIKEは文字列パターン照合用の演算子で、NULL判定には使いません。","SQLではNULLとの比較に=を使わず、IS NULLを使います。 NULLは「値が存在しない/不明」を表すため = NULL では真になりません。NULL判定専用のIS NULLを使います。"]},"db-14":{"choiceExps":["降順はDESC、昇順はASCです。高い値から並べるのでDESCを指定します。 標準SQLではSORT HIGHという構文ではなく、ORDER BY列名 DESCで降順を指定します。","降順はDESC、昇順はASCです。高い値から並べるのでDESCを指定します。 ASCは昇順なので低い値から並びます。高い順にはDESCが必要です。","降順はDESC、昇順はASCです。高い値から並べるのでDESCを指定します。","降順はDESC、昇順はASCです。高い値から並べるのでDESCを指定します。 GROUP BYは同じ値を集約単位にまとめる句で、並べ替えを指定する句ではありません。"]},"db-15":{"choiceExps":["COMMITはトランザクション内で行った更新を確定し、永続的な結果として扱います。 更新を取り消す操作はROLLBACKです。COMMITは逆に更新を確定します。","COMMITはトランザクション内で行った更新を確定し、永続的な結果として扱います。 表の削除はDROP TABLEなどのDDLで行う操作で、COMMITの意味ではありません。","COMMITはトランザクション内で行った更新を確定し、永続的な結果として扱います。 COMMITは更新を確定してトランザクションを終える操作で、DBMSの排他制御機能そのものを無効化する命令ではありません。","COMMITはトランザクション内で行った更新を確定し、永続的な結果として扱います。"]},"db-16":{"choiceExps":["ROLLBACKは、まだ確定していないトランザクションの更新を取り消します。","ROLLBACKは、まだ確定していないトランザクションの更新を取り消します。 CHECKPOINTは障害回復のためにDBの状態やログ上の区切りを記録する処理で、未確定更新を取り消す操作ではありません。この設問で必要なのは未確定の更新を取り消すROLLBACKです。","ROLLBACKは、まだ確定していないトランザクションの更新を取り消します。 GRANTは利用者などへ権限を付与するSQL文で、トランザクションの更新を取り消す操作ではありません。この設問で必要なのは未確定の更新を取り消すROLLBACKです。","「COMMIT」は、トランザクションで行った変更を正式に確定する操作です。 この設問では「COMMITの反対の操作を選びます」が判断軸で、該当するのは「ROLLBACK」です。"]},"db-17":{"choiceExps":["未確定の変更を他トランザクションが読み取ることをダーティリードといいます。 ファントムリードは同じ範囲検索を再実行したとき他トランザクションの追加・削除で行集合が変化する現象で、未COMMIT値を読む現象ではありません。この設問で必要なのは未COMMITの変更を別トランザクションが読むダーティリードです。","未確定の変更を他トランザクションが読み取ることをダーティリードといいます。","未確定の変更を他トランザクションが読み取ることをダーティリードといいます。 デッドロックは複数の処理が互いに資源解放を待って進めなくなる状態で、ページ不在や単なる入出力待ちとは別の現象です。この設問で必要なのは未COMMITの変更を別トランザクションが読むダーティリードです。","未確定の変更を他トランザクションが読み取ることをダーティリードといいます。 ロストアップデートは複数更新が競合して一方の更新結果が上書きされ失われる現象で、未確定値を読むダーティリードとは異なります。この設問で必要なのは未COMMITの変更を別トランザクションが読むダーティリードです。"]},"db-18":{"choiceExps":["注文IDだけでは同じ注文内の複数明細を区別できません。注文IDと商品IDの組合せなら各明細を識別できます。 同じ注文に複数商品があるため注文IDだけでは複数行が同じ値になり、明細を一意に識別できません。","注文IDだけでは同じ注文内の複数明細を区別できません。注文IDと商品IDの組合せなら各明細を識別できます。 商品名や価格は変更・重複し得る業務データで、注文そのものを特定する注文IDも含まれないため明細識別に不安定です。","注文IDだけでは同じ注文内の複数明細を区別できません。注文IDと商品IDの組合せなら各明細を識別できます。","注文IDだけでは同じ注文内の複数明細を区別できません。注文IDと商品IDの組合せなら各明細を識別できます。 数量は多くの明細で同じ値になり得るため、一意な識別子にはなりません。"]},"db-19":{"choiceExps":["第2正規形では、非キー属性が複合主キーの一部だけに依存する部分関数従属を取り除きます。 第3正規形で除く中心は非キー属性間の推移的関数従属です。複合主キーの一部への依存を除くのは第2正規形です。","第2正規形では、非キー属性が複合主キーの一部だけに依存する部分関数従属を取り除きます。 非正規形は繰返し項目などが整理される前の状態で、部分関数従属を除去した状態を表しません。","第2正規形では、非キー属性が複合主キーの一部だけに依存する部分関数従属を取り除きます。 第1正規形は各属性を原子的な値にする段階です。部分関数従属の除去はその次の第2正規形です。","第2正規形では、非キー属性が複合主キーの一部だけに依存する部分関数従属を取り除きます。"]},"db-20":{"choiceExps":["インデックスは検索を速くできる一方、データ更新時にはインデックス側も更新する必要があり、領域も消費します。","「トランザクション」は、複数のデータ操作を、まとめて成功または失敗させる処理単位です。 この設問では「検索性能だけでなく、データを書き換えるときのコストを考えます」が判断軸で、該当するのは「INSERTやUPDATE時の更新負荷と保存領域が増える」です。","インデックスは検索を速くできる一方、データ更新時にはインデックス側も更新する必要があり、領域も消費します。 インデックスはSELECT検索を速めるために利用でき、増やしすぎてもSELECT自体が実行不能になるわけではありません。問題になるのは更新時の索引更新コストと保存領域です。","「外部キー」は、別の表の主キーなどを参照し、表同士を関連付けるための列です。 この設問では「検索性能だけでなく、データを書き換えるときのコストを考えます」が判断軸で、該当するのは「INSERTやUPDATE時の更新負荷と保存領域が増える」です。"]}};
Object.keys(SUBJECT_A_V138_OVERRIDES_3).forEach(qid=>{const q=QUESTION_BANK.find(x=>x.id===qid);if(!q)throw new Error(`v138 missing question: ${qid}`);const a=q.a,id=q.id;Object.assign(q,SUBJECT_A_V138_OVERRIDES_3[qid],{qualityAudit:'v138-condition-specific-pass'});if(q.a!==a)throw new Error(`v138 answer-index drift: ${qid}`);if(q.id!==id)throw new Error(`v138 id drift: ${qid}`);});
const SUBJECT_A_V138_IDS_3=["db-01","db-02","db-03","db-05","db-06","db-07","db-08","db-11","db-13","db-14","db-15","db-16","db-17","db-18","db-19","db-20"];
