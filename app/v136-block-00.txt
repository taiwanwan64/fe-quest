// ===== v136 specific distractor pass: 基礎理論 =====
const SUBJECT_A_V136_SPECIFIC_OVERRIDES_0={"coreq_02_03_1":{"choiceExps":["「AB+」が正解です。後置記法はAB+。括弧を含む中置式から演算順を確定する。","「+AB」は演算子を先頭に置く前置記法です。後置記法では被演算子A、Bの後ろに演算子+を置きます。","「A+B」は演算子を被演算子の間に置く通常の中置記法です。後置記法では+を末尾へ移します。","「BA+」は演算子を末尾に置いていますが、被演算子AとBの順序が逆です。A+Bの後置記法はAB+です。"]},"coreq_02_04_1":{"choiceExps":["入力前は状態Aですが、規則で「Aに1を入力するとBへ遷移する」と定義されているため、入力後もAのままではありません。","「B」が正解です。状態Aで1ならB、Bで1ならAという規則なら入力11でAへ戻ります。入力を左から一つずつ適用する。","問題文には入力1で状態Bへ遷移する規則があり、そこで処理が終了するとは定められていません。","状態Aで入力1を受けたときの遷移先Bが明示されているため、状態は不定ではありません。"]},"coreq_02_05_1":{"choiceExps":["「教師あり学習」は正解ラベル付きデータ。分類・回帰など。一方「教師なし学習」は正解ラベルなし。クラスタリングなど。この問題で求めるのは前者である。","「教師あり学習」が正解です。教師あり学習は正解ラベルを使う。過学習・学習データの偏りを理解する。","「教師あり学習」は正解ラベル付きデータ。分類・回帰など。一方「強化学習」は行動結果の報酬。試行錯誤で方策を改善。この問題で求めるのは前者である。","暗号化はデータを第三者に読まれにくくするセキュリティ技術で、正解ラベルを使ってモデルを学習させる方式ではありません。"]},"coreq_02_06_1":{"choiceExps":["「標準偏差」が正解です。標準偏差 = √分散。標準偏差が大きいほどばらつきが大きい。順序を区別するかで順列と組合せを判断する。","中央値はデータを順に並べたときの中央の値で、分布の中心を表す代表値です。ばらつきそのものを表す指標ではありません。","相関係数は二つの変数の関係の強さや向きを表します。一つのデータ集合の散らばりを表す標準偏差とは役割が異なります。","回帰式は変数間の関係を式で表し、予測などに使います。データの散らばりの大きさを直接表す指標ではありません。"]},"coreq_02_07_1":{"choiceExps":["「二分法」が正解です。二分法は解を含む区間を半分ずつ狭めて近似解を求める。近似法では「何を繰り返して範囲を狭めるか」を見る。","バブルソートは隣接要素を比較・交換してデータを整列するアルゴリズムで、方程式の解を含む区間を狭める方法ではありません。","「探索」は、目的のデータをデータ集合の中から探す処理です。 この問題で求める内容とは一致しない。","ラウンドロビンは各処理へ一定時間ずつCPUを割り当てるスケジューリング方式で、方程式の近似解を求める数値計算法ではありません。"]},"coreq_02_08_1":{"choiceExps":["「ハフマン符号」が正解です。ハフマン符号は出現頻度の高い記号へ短い符号を割り当てる可変長符号の代表例。確率1/2の事象の情報量は1bit。","RSAは公開鍵暗号方式で、鍵を用いて暗号化・復号などを行います。記号の出現頻度から可変長符号を割り当てる圧縮方式ではありません。","ラウンドロビンはCPU時間などを順番に割り当てる方式で、データ圧縮の符号化方式ではありません。","RAIDは複数ディスクを組み合わせて冗長性や性能を高める技術で、出現頻度に応じて符号長を変える方式ではありません。"]}};
Object.keys(SUBJECT_A_V136_SPECIFIC_OVERRIDES_0).forEach(qid=>{const q=QUESTION_BANK.find(x=>x.id===qid);if(!q)throw new Error(`v136 missing question: ${qid}`);const patch=SUBJECT_A_V136_SPECIFIC_OVERRIDES_0[qid];const answerBefore=q.a;const idBefore=q.id;Object.assign(q,patch,{qualityAudit:'v136-specific-distractor-pass'});if(q.a!==answerBefore)throw new Error(`v136 answer-index drift: ${qid}`);if(q.id!==idBefore)throw new Error(`v136 id drift: ${qid}`);});
