name: FE QUEST v162 release validation

on:
  push:
    branches: [v162-staging]
  workflow_dispatch:

permissions:
  contents: read
  pages: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Build Jekyll site
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - name: Validate FE QUEST v162
        run: python3 tools/v162_validate.py
      - name: Package FE QUEST v162
        run: |
          cd _site
          zip ../FE_QUEST_PWA_v162.zip index.html manifest.webmanifest sw.js icon-192.png icon-512.png apple-touch-icon.png
      - name: Upload FE QUEST v162
        uses: actions/upload-artifact@v4
        with:
          name: fe-quest-v162-standalone
          path: FE_QUEST_PWA_v162.zip
          if-no-files-found: error
