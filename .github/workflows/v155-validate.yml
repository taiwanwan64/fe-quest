name: FE QUEST v155 release validation

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Build Jekyll site
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - name: Validate FE QUEST v155
        run: python3 tools/v155_validate_actual.py
      - name: Package FE QUEST v155
        run: |
          cd _site
          zip ../FE_QUEST_PWA_v155.zip index.html manifest.webmanifest sw.js icon-192.png icon-512.png apple-touch-icon.png
      - name: Upload FE QUEST v155
        uses: actions/upload-artifact@v4
        with:
          name: fe-quest-v155-standalone
          path: FE_QUEST_PWA_v155.zip
          if-no-files-found: error
