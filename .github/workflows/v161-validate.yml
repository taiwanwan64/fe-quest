name: FE QUEST v161 release validation

on:
  push:
    branches: [v161-staging]
  workflow_dispatch:

permissions:
  contents: read
  pages: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Build Jekyll site
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - name: Validate FE QUEST v161
        run: python3 tools/v161_validate.py
      - name: Package FE QUEST v161
        run: |
          cd _site
          zip ../FE_QUEST_PWA_v161.zip index.html manifest.webmanifest sw.js icon-192.png icon-512.png apple-touch-icon.png
      - name: Upload FE QUEST v161
        uses: actions/upload-artifact@v4
        with:
          name: fe-quest-v161-standalone
          path: FE_QUEST_PWA_v161.zip
          if-no-files-found: error
