{
  "version": "v340",
  "freshProfile": {
    "ok": true,
    "value": {
      "settings": {
        "studyMinutes": 60,
        "examDate": "",
        "autoPace": true,
        "variantReview": true
      },
      "xp": 0,
      "streak": 0,
      "sessionCount": 0,
      "lessonDone": 0,
      "bDone": 0
    }
  },
  "tasks": {
    "ok": true,
    "value": [
      {
        "type": "review",
        "title": "記憶の復習",
        "minutes": 10,
        "bmode": null,
        "bid": null,
        "lessonId": null
      },
      {
        "type": "lesson",
        "title": "データの単位",
        "minutes": 20,
        "bmode": null,
        "bid": null,
        "lessonId": "core_01_01"
      },
      {
        "type": "subjectB",
        "title": "プログラムトレース：ループで合計",
        "minutes": 20,
        "bmode": "trace",
        "bid": "loop_sum",
        "lessonId": null
      },
      {
        "type": "boss",
        "title": "今日の総合チェック",
        "minutes": 10,
        "bmode": null,
        "bid": null,
        "lessonId": null
      }
    ]
  },
  "snapshot": {
    "ok": true,
    "value": [
      {
        "type": "review",
        "title": "記憶の復習",
        "minutes": 10,
        "bmode": null,
        "bid": null,
        "lessonId": null
      },
      {
        "type": "lesson",
        "title": "データの単位",
        "minutes": 20,
        "bmode": null,
        "bid": null,
        "lessonId": "core_01_01"
      },
      {
        "type": "subjectB",
        "title": "プログラムトレース：ループで合計",
        "minutes": 20,
        "bmode": "trace",
        "bid": "loop_sum",
        "lessonId": null
      },
      {
        "type": "boss",
        "title": "今日の総合チェック",
        "minutes": 10,
        "bmode": null,
        "bid": null,
        "lessonId": null
      }
    ]
  },
  "functions": {
    "renderHome": null,
    "renderTodayPlan": null,
    "buildTodayTasks": {
      "bytes": 2837,
      "source": "function buildTodayTasks(){\n  const daysNow=examDaysRemaining();\n  const totalNow=effectiveStudyMinutes();\n  if(daysNow!=null&&daysNow>=0&&daysNow<=7)return buildTaperTodayTasks(daysNow,totalNow);\n  const alloc=taskAllocation(totalNow);\n  const routes=actionableReviewJourneys().length;\n  const due=dueQuestions().length;\n  const lesson=nextLessonChoice();\n  const b=nextBChoice(alloc.subjectB);\n  const bMinutes=(b.mode==='compound'||b.mode==='securityMock')?Math.min(20,alloc.subjectB):alloc.subjectB;\n  const lessonMinutes=alloc.lesson+Math.max(0,alloc.subjectB-bMinutes);\n  const weak=weakestSkill();\n  const days=examDaysRemaining();\n  const finalPhase=days!=null&&days<=14;\n\n  const rxReady=window.FEQUEST_RX_READY===true;\n  const rx=rxReady?recommendedPrescription():{kind:'knowledge',cat:lesson.skill,reason:'データ不足'};\n  const rxMeta=rxReady?prescriptionMeta(rx):{icon:'📘',title:lesson.title,desc:'未完了教材を優先'};\n  const useRx=rxReady && rx.kind!=='knowledge';\n\n  let learnTask;\n  if(lesson.complete){\n    const focus=shortFinalFocusRecommendation();\n    learnTask={\n      type:'finalFocus',icon:focus.icon,bg:'blue',\n      title:focus.title,desc:focus.desc,\n      minutes:lessonMinutes,focus\n    };\n  }else if(useRx){\n    learnTask={\n      type:'prescription',icon:rxMeta.icon,bg:'blue',\n      title:rxMeta.title,desc:`原因「${rx.reason}」に合わせて自動処方`,\n      minutes:lessonMinutes,rx\n    };\n  }else{\n    learnTask={\n      type:'lesson',icon:'📘',bg:'blue',\n      title:lesson.title,desc:`${lesson.skill}・未完了教材を優先`,\n      minutes:lessonMinutes,lessonId:lesson.id\n    };\n  }\n\n  return [\n    {\n      type:'review',icon:'🧠',bg:'green',\n      title:'記憶の復習',\n      desc:routes?`復習ルート ${routes}件を優先`:due?`復習期限の問題 ${due}問を優先`:`今日の復習期限はなし。「${weak}」を補強`,\n      minutes:alloc.review\n    },\n    learnTask,\n    {\n      type:'subjectB',icon:'💻',bg:'purple',\n      title:b.mode==='trace'?`プログラムトレース：${b.title}`:b.mode==='security'?`セキュリティ：${b.title}`:b.mode==='compound'?`複合演習：${b.title}`:b.mode==='securityMock'?b.title:`実戦：${b.title}`,\n      desc:b.mode==='trace'?'科目B アルゴリズム':b.mode==='security'?'科目B 情報セキュリティ':b.mode==='compound'?'科目B アルゴリズム・20分実戦':b.mode==='securityMock'?'科目B セキュリティ・20分実戦':'科目B 実戦',\n      minutes:bMinutes,bmode:b.mode,bid:b.id\n    },\n    {\n      type:'boss',icon:'👑',bg:'yellow',\n      title:finalPhase?'直前総合チェック':'今日の総合チェック',\n      desc:finalPhase?'判断3問＋適用2問・分野を散らして5問':'全分野から5問・仕上げチェック',\n      minutes:alloc.boss\n    }\n  ];\n}"
    },
    "effectiveStudyMinutes": {
      "bytes": 145,
      "source": "function effectiveStudyMinutes(){const p=examPaceStatus();return p.hasExam&&!p.expired?p.effective:(Number(profile.settings?.studyMinutes)||60);}"
    },
    "examDaysRemaining": {
      "bytes": 214,
      "source": "function examDaysRemaining(){\n  const val=profile.settings?.examDate;if(!val)return null;\n  const target=new Date(val+'T12:00:00'),now=new Date();now.setHours(12,0,0,0);\n  return Math.ceil((target-now)/86400000);\n}"
    },
    "taskAllocation": {
      "bytes": 516,
      "source": "function taskAllocation(total){\n  total=Number(total)||effectiveStudyMinutes();const days=examDaysRemaining();\n  if(days!=null&&days>=0&&days<=6)return taperTaskAllocation(total,days);\n  if(days==null){if(total===30)return {review:5,lesson:10,subjectB:10,boss:5};if(total===45)return {review:5,lesson:15,subjectB:15,boss:10};if(total===90)return {review:15,lesson:30,subjectB:30,boss:15};if(total===60)return {review:10,lesson:20,subjectB:20,boss:10};}\n  return allocateByRatios(total,examStudyPhase(days).ratios);\n}"
    },
    "saveProfile": {
      "bytes": 1921,
      "source": "function saveProfile(){\n  // v117: top-level schema/backfill calls made while the one-file bundle is still\n  // declaring later modules must never refresh the whole UI. Defer them into one\n  // final save after every dataset/renderer exists.\n  if(!appBootComplete){bootProfileSavePending=true;return true;}\n  if(profileWriteBlocked||profileConflictBlocked){restoreCommittedProfileInMemory(true);return false;}\n  if(!acquireProfileWriteLease()){\n    restoreCommittedProfileInMemory(true);\n    markProfileConflict('別のFE QUEST画面がいま学習データを書き込んでいます');return false;\n  }\n  let saved=false;\n  try{\n    profile=stampProfileForSave(profile);\n    const result=writeCurrentProfile(profile,{preservePrevious:true});\n    profile=result.profile;rememberCommittedProfile(profile);clearProfileSaveFailure();\n    saved=true;\n  }catch(e){\n    restoreCommittedProfileInMemory(false);noteProfileSaveFailure(e);\n    if(e?.code==='PROFILE_REVISION_CONFLICT'){markProfileConflict();return false}\n    profileWriteBlocked=true;\n    console.warn('学習データの保存に失敗しました。',e);\n    setTimeout(()=>showAppNotice('error','保存できませんでした','現在の学習データは最後に正常保存できた状態へ戻しました。ページを再読み込みすると改善する場合があります。','再読み込み',()=>location.reload()),0);\n    setTimeout(()=>refreshPwaHealth?.(),0);\n    return false;\n  }finally{releaseProfileWriteLease()}\n\n  // A recovery-checkpoint or UI-rendering bug must never be misclassified as a\n  // storage failure. Persistence has already succeeded at this point.\n  try{queueRecoveryCheckpoint('autosave',false)}catch(e){console.warn('Recovery checkpoint queue failed',e)}\n  try{refreshProfileUI()}catch(e){\n    console.warn('UI refresh failed after a successful save',e);\n    setTimeout(()=>reportGlobalError?.(),0);\n  }\n  return saved;\n}"
    },
    "showScreen": {
      "bytes": 960,
      "source": "function showScreen(id,opts={}){\n  const exists=screens.some(s=>s.id===id);\n  if(!exists)id='home';\n  const prev=activeScreenId();\n  screens.forEach(s => {\n    const active=s.id===id;\n    s.classList.toggle('active',active);\n    s.setAttribute('aria-hidden',active?'false':'true');\n  });\n  const navRoot=primaryNavScreen(id);\n  navBtns.forEach(b => {\n    const active=b.dataset.screen===navRoot;\n    b.classList.toggle('active',active);\n    if(active)b.setAttribute('aria-current','page');else b.removeAttribute('aria-current');\n  });\n  if(id==='history' && typeof renderLearningAnalytics==='function') renderLearningAnalytics();\n  if(id==='map' && typeof renderLearningEntry==='function') renderLearningEntry();\n  rememberScreen(id);\n  if(!opts.fromHistory && !opts.noHistory && prev!==id)appHistoryPush(id);\n  if(opts.replaceHistory)appHistoryReplace(id,opts.depth||0);\n  if(!opts.keepScroll)window.scrollTo({top:0, behavior:opts.instant?'auto':'smooth'});\n}"
    },
    "openPlan": null,
    "renderProfile": null,
    "refreshProfileUI": {
      "bytes": 66,
      "source": "function(){\n  _refreshProfileUIV18();\n  renderMemoryDashboard();\n}"
    },
    "ensureTodayPlanSnapshot": {
      "bytes": 766,
      "source": "function ensureTodayPlanSnapshot(force=false){\n  const rec=getDailyRecord();\n  const stale=rec.planVersion!=='v98';\n  if(force||!Array.isArray(rec.tasks)||!rec.tasks.length||stale){\n    const old=snapshotDailyTasks(Array.isArray(rec.tasks)?rec.tasks:[]);\n    const fresh=snapshotDailyTasks(buildTodayTasks());\n    const done=rec.done||{};\n    rec.tasks=fresh.map(t=>{\n      const slot=dailyTaskSlot(t);\n      if(done[slot])return old.find(x=>dailyTaskSlot(x)===slot)||t;\n      return t;\n    });\n    rec.targetMinutes=rec.tasks.reduce((s,t)=>s+(Number(t.minutes)||0),0);\n    rec.planVersion='v98';\n    rec.plannedAt=rec.plannedAt||new Date().toISOString();\n    if(stale)rec.replannedAt=new Date().toISOString();\n    persistProfileSilently();\n  }\n  return rec.tasks;\n}"
    }
  },
  "domControls": [],
  "snippets": {
    "examDate": [
      ".getElementById('secLab')?.classList.remove('show'); document.querySelectorAll('[data-bmode]').forEach(b=>b.classList.toggle('active',b.dataset.bmode==='trace')); _showBListV9(); }; // ===== v10: Adaptive daily scheduler / roadmap ===== if(!profile.settings) profile.settings={studyMinutes:60,examDate:''}; if(!profile.dailyPlans) profile.dailyPlans={}; if(!profile.activity) profile.activity={}; if(!profile.settings.studyMinutes) profile.settings.studyMinutes=60; if(profile.settings.autoPace==null) profile.settings.autoPace=true; if(profile.settings.variantReview==null) profile.settings.variantReview=true; saveProfile(); function dateKey(d=new Date()){ const x=new Date(d); const y=x.getFullYear(); const m=String(x.getMonth()+1).padStart(2,'0'); const day=String(x.getDate()).padStart(2,'0'); return `${y}-${m}-${day}`; } function addActivity(minutes,type='study'){ const k=dateKey(); if(!profile.activity[k]) profile.activity[k]={minutes:0,events:[]}; profile.activity",
      "Html(currentFinalFocus.desc)}${currentFinalFocus.long?'<br><span class=\"sub\">通常クエストへ上乗せせず、その日のメイン学習としてまとまった時間を確保する実戦メニューです。</span>':''}`; btn.textContent=currentFinalFocus.button; btn.onclick=()=>launchFinalFocus(currentFinalFocus); } function examDaysRemaining(){ const val=profile.settings?.examDate;if(!val)return null; const target=new Date(val+'T12:00:00'),now=new Date();now.setHours(12,0,0,0); return Math.ceil((target-now)/86400000); } function examStudyPhase(days=examDaysRemaining()){ if(days==null)return {id:'normal',name:'通常学習',icon:'📘',ratios:{review:.17,lesson:.33,subjectB:.33,boss:.17},allowNew:true,allowLongExam:true}; if(days<0)return {id:'expired',name:'受験日経過',icon:'📅',ratios:{review:.17,lesson:.33,subjectB:.33,boss:.17},allowNew:true,allowLongExam:true}; if(days>90)return {id:'foundation',name:'基礎拡張期',icon:'🌱',ratios:{review:.15,lesson:.35,subjectB:.30,boss:.20},allowNew:true,allowLongExam:true}; if(days>30)return {id:'build',name:'実力形成期',icon:'🧩',ratio",
      "すめです。` : ' 復習待ちはないので、新しい教材を進めて構いません。'; document.getElementById('adaptiveNote').innerHTML=`<b>FE QUESTの提案：</b> ${focus}${reviewText}`; } renderFinalMasterPanel(); } function renderCountdown(){ const box=document.getElementById('countdownBox');if(!box)return; if(!profile.settings.examDate){box.innerHTML='<div class=\"countdown-empty\">受験予定日を設定すると、残り日数・必要ペース・完了見込みを計算します。</div>';return;} const days=examDaysRemaining(); if(days<0)box.innerHTML='<div class=\"countdown-empty\">設定した受験予定日は過ぎています。新しい日付を設定してください。</div>'; else if(days===0)box.innerHTML='<div class=\"countdown\"><div class=\"countdown-num\">今日</div><div class=\"countdown-label\">受験予定日・新規学習は増やさない</div></div>'; else if(days===1)box.innerHTML='<div class=\"countdown\"><div class=\"countdown-num\">1</div><div class=\"countdown-label\">日前・短い復習だけで整える</div></div>'; else box.innerHTML=`<div class=\"countdown\"><div class=\"countdown-num\">${days}</div><div class=\"countdown-label\">日後までに仕上げる</div></div>`; } function paceDateText(",
      "nt='今日の計画は完了しています';desc.textContent='追加で詰め込まず、必要なら演習や分析を確認できます。';icon.textContent='✓';action.textContent='ホームへ →';action.disabled=false;action.onclick=()=>showScreen('home');} } function renderPlannerScreen(){ const mins=document.getElementById('studyMinutesSetting'),date=document.getElementById('examDateSetting'),auto=document.getElementById('autoPaceSetting'); if(mins)mins.value=String(profile.settings.studyMinutes||60);if(date)date.value=profile.settings.examDate||'';if(auto)auto.checked=profile.settings.autoPace!==false; renderRoadmap();renderWeekChart();renderReadiness();renderCountdown();renderExamPace();renderAllocation();renderDailyPlan();renderPlanFocus(); document.getElementById('examPaceCard')?.classList.toggle('hidden-by-plan',!profile.settings.examDate); } document.getElementById('savePlanSettings')?.addEventListener('click',()=>{ profile.settings.studyMinutes=Number(document.getElementById('studyMinutesSetting').value)||60; profile.settings.examDate=document.",
      "'home');} } function renderPlannerScreen(){ const mins=document.getElementById('studyMinutesSetting'),date=document.getElementById('examDateSetting'),auto=document.getElementById('autoPaceSetting'); if(mins)mins.value=String(profile.settings.studyMinutes||60);if(date)date.value=profile.settings.examDate||'';if(auto)auto.checked=profile.settings.autoPace!==false; renderRoadmap();renderWeekChart();renderReadiness();renderCountdown();renderExamPace();renderAllocation();renderDailyPlan();renderPlanFocus(); document.getElementById('examPaceCard')?.classList.toggle('hidden-by-plan',!profile.settings.examDate); } document.getElementById('savePlanSettings')?.addEventListener('click',()=>{ profile.settings.studyMinutes=Number(document.getElementById('studyMinutesSetting').value)||60; profile.settings.examDate=document.getElementById('examDateSetting').value||''; profile.settings.autoPace=!!document.getElementById('autoPaceSetting')?.checked; saveProfile(); rebuildTodayPlanPre"
    ],
    "studyMinutes": [
      "en'); document.getElementById('secLab')?.classList.remove('show'); document.querySelectorAll('[data-bmode]').forEach(b=>b.classList.toggle('active',b.dataset.bmode==='trace')); _showBListV9(); }; // ===== v10: Adaptive daily scheduler / roadmap ===== if(!profile.settings) profile.settings={studyMinutes:60,examDate:''}; if(!profile.dailyPlans) profile.dailyPlans={}; if(!profile.activity) profile.activity={}; if(!profile.settings.studyMinutes) profile.settings.studyMinutes=60; if(profile.settings.autoPace==null) profile.settings.autoPace=true; if(profile.settings.variantReview==null) profile.settings.variantReview=true; saveProfile(); function dateKey(d=new Date()){ const x=new Date(d); const y=x.getFullYear(); const m=String(x.getMonth()+1).padStart(2,'0'); const day=String(x.getDate()).padStart(2,'0'); return `${y}-${m}-${day}`; } function addActivity(minutes,type='study'){ const k=dateKey(); if(!profile.activity[k]) profile.activity[k]={minutes:0,events:[]}; ",
      "e('active',b.dataset.bmode==='trace')); _showBListV9(); }; // ===== v10: Adaptive daily scheduler / roadmap ===== if(!profile.settings) profile.settings={studyMinutes:60,examDate:''}; if(!profile.dailyPlans) profile.dailyPlans={}; if(!profile.activity) profile.activity={}; if(!profile.settings.studyMinutes) profile.settings.studyMinutes=60; if(profile.settings.autoPace==null) profile.settings.autoPace=true; if(profile.settings.variantReview==null) profile.settings.variantReview=true; saveProfile(); function dateKey(d=new Date()){ const x=new Date(d); const y=x.getFullYear(); const m=String(x.getMonth()+1).padStart(2,'0'); const day=String(x.getDate()).padStart(2,'0'); return `${y}-${m}-${day}`; } function addActivity(minutes,type='study'){ const k=dateKey(); if(!profile.activity[k]) profile.activity[k]={minutes:0,events:[]}; profile.activity[k].minutes += Number(minutes)||0; profile.activity[k].events.push({type,time:new Date().toISOString()}); profile.activit",
      "race')); _showBListV9(); }; // ===== v10: Adaptive daily scheduler / roadmap ===== if(!profile.settings) profile.settings={studyMinutes:60,examDate:''}; if(!profile.dailyPlans) profile.dailyPlans={}; if(!profile.activity) profile.activity={}; if(!profile.settings.studyMinutes) profile.settings.studyMinutes=60; if(profile.settings.autoPace==null) profile.settings.autoPace=true; if(profile.settings.variantReview==null) profile.settings.variantReview=true; saveProfile(); function dateKey(d=new Date()){ const x=new Date(d); const y=x.getFullYear(); const m=String(x.getMonth()+1).padStart(2,'0'); const day=String(x.getDate()).padStart(2,'0'); return `${y}-${m}-${day}`; } function addActivity(minutes,type='study'){ const k=dateKey(); if(!profile.activity[k]) profile.activity[k]={minutes:0,events:[]}; profile.activity[k].minutes += Number(minutes)||0; profile.activity[k].events.push({type,time:new Date().toISOString()}); profile.activity[k].events=profile.activity[k]",
      "0:00');observedDays=Math.min(days,Math.max(1,Math.floor((today-first)/86400000)+1));} return {total,activeDays,observedDays,enough:activeDays>=3&&observedDays>=4,avg:observedDays?total/observedDays:0}; } function examPaceStatus(){ const days=examDaysRemaining(),baseline=Number(profile.settings?.studyMinutes)||60,remaining=estimateRemainingStudyMinutes(),recent=recentCalendarPace(),phase=examStudyPhase(days); if(days==null)return {hasExam:false,baseline,effective:baseline,remaining,recent,phase}; if(days<0)return {hasExam:true,expired:true,days,baseline,effective:baseline,remaining,recent,phase}; const available=Math.max(1,days),required=remaining/available,currentPace=recent.enough?recent.avg:baseline,paceSource=recent.enough?'recent':'plan'; const projectedDays=currentPace>0?Math.ceil(remaining/currentPace):9999,projectedDate=new Date();projectedDate.setHours(12,0,0,0);projectedDate.setDate(projectedDate.getDate()+projectedDays); const slack=available-projectedDays; le",
      "maining,recent,phase,required,currentPace,paceSource,projectedDays,projectedDate,slack,status,auto,effective,canRecover:adjustedDays<=available,maxDate,taper,taperCap:cap}; } function effectiveStudyMinutes(){const p=examPaceStatus();return p.hasExam&&!p.expired?p.effective:(Number(profile.settings?.studyMinutes)||60);} function taperTaskAllocation(total,days){ total=Math.max(5,Math.round(Number(total)||10)); if(days<=0)return {review:0,lesson:0,subjectB:0,boss:total}; if(days===1)return {review:Math.max(5,total-5),lesson:0,subjectB:0,boss:Math.min(5,total)}; if(days<=3)return {review:10,lesson:Math.max(5,total-15),subjectB:0,boss:5}; if(total<=30)return {review:10,lesson:10,subjectB:5,boss:5}; return {review:15,lesson:10,subjectB:10,boss:Math.max(5,total-35)}; } function taskAllocation(total){ total=Number(total)||effectiveStudyMinutes();const days=examDaysRemaining(); if(days!=null&&days>=0&&days<=6)return taperTaskAllocation(total,days); if(days==null){if(total===30"
    ],
    "autoAdjust": [],
    "renderHome": [
      "gLabel(stat){ const due=stat?.due; if(!due) return {text:'復習タイミング：自動調整',due:false}; const today=localDateISO(0); if(due<=today) return {text:'復習タイミング：今',due:true}; const days=Math.max(1,daysBetweenISO(today,due)); return {text:days===1?'次回の復習：明日':`次回の復習：${days}日後`,due:false}; } function renderHomeReviewCandidates(){ const root=document.getElementById('homeReviewCandidates'); if(!root) return; const items=topReviewCandidates(3); if(!items.length){ root.innerHTML='<div class=\"mini-card\"><div class=\"big\">🧠</div><b>学習データ待ち</b><div class=\"sub\">問題を解くと、忘れそうな論点をここに表示します。</div></div>'; return; } const icons={'基礎理論':'🔢','コンピュータ':'⚙️','データベース':'🗃️','ネットワーク':'🌐','セキュリティ':'🛡️','アルゴリズム':'🧩','マネジメント':'📋','ストラテジ':'📈'}; root.innerHTML=items.map(q=>{ const st=profile.qStats[q.id]; const r=memoryRetention(st)??100; const timing=reviewTimingLabel(st); return `<div class=\"mini-card\"><div class=\"big\">${icons[q.cat]||'🧠'}</div><b>${escapeHtml(q.concept)}</b> ",
      "<div class=\"forecast-count\">${x.count?x.count+'問':''}</div> <div class=\"forecast-bar-wrap\"><div class=\"forecast-bar\" style=\"height:${height}%\"></div></div> <div class=\"forecast-label\">${i===0?'今日':names[d.getDay()]}</div> </div>`; }).join(''); } function renderMemoryDashboard(){ renderHomeReviewCandidates(); renderMemoryHealth(); renderReviewForecast(); updateDueCount(); renderReviewJourneyHub(); } document.getElementById('startForecastReview')?.addEventListener('click',()=>{ showScreen('problems'); startQuiz('review'); }); const _renderPlannerScreenV18=renderPlannerScreen; renderPlannerScreen=function(){ _renderPlannerScreenV18(); renderMemoryDashboard(); }; const _refreshProfileUIV18=refreshProfileUI; refreshProfileUI=function(){ _refreshProfileUIV18(); renderMemoryDashboard(); }; renderMemoryDashboard(); // ===== v19: Variant Review settings ===== function refreshVariantReviewUI(){ const t=document.getElementById('variantReviewToggle');"
    ],
    "renderTodayPlan": [],
    "学習時間": [
      " ok:true, profile:structuredClone(profile), settings:structuredClone(profile.settings||{}), recoveryPointCreated }; } async function requestLearningDataResetV333(){ const first=window.confirm( 'FE QUESTを最初からやり直します。\\n\\n'+ '初期化されるもの：教材の進捗、問題履歴、復習、模試履歴、科目B進捗、XP、ストリーク、試験予定日、1日の学習時間、自動調整、学習計画など\\n\\n'+ '初期化後は試験準備度や必要ペースなども、初回利用時の状態から再計算されます。\\n\\n'+ '安全のため、端末内の復旧点と書き出したバックアップは残ります。誤って初期化した場合だけ復旧センターから戻せます。続けますか？' ); if(!first)return false; const typed=window.prompt('最終確認です。FE QUESTを初期状態へ戻すには「初期化」と入力してください。',''); if(typed!==LEARNING_DATA_RESET_CONFIRM_TEXT_V333){ if(typed!==null)showAppNotice('info','初期化を中止しました','確認文字が一致しなかったため、データは変更していません。'); return false; } const result=await performLearningDataResetV333(); if(!result.ok){ showAppNotice('error','初期化できませんでした','データは変更していません。ほかのFE QUEST画面を閉じてから、もう一度お試しください。'); return false; } window.alert('FE QUESTを初期状態に戻しました。試験予定日や学習時間などの設定もリセットされています。'); location.reload(); return true; }",
      "info','初期化を中止しました','確認文字が一致しなかったため、データは変更していません。'); return false; } const result=await performLearningDataResetV333(); if(!result.ok){ showAppNotice('error','初期化できませんでした','データは変更していません。ほかのFE QUEST画面を閉じてから、もう一度お試しください。'); return false; } window.alert('FE QUESTを初期状態に戻しました。試験予定日や学習時間などの設定もリセットされています。'); location.reload(); return true; } function installLearningDataResetV333(){ if(document.getElementById('resetLearningDataV333'))return true; const host=document.querySelector('#pwaHealthCard .pwa-health-actions-advanced'); if(!host)return false; if(!document.getElementById('learningDataResetStyleV333')){ const style=document.createElement('style'); style.id='learningDataResetStyleV333'; style.textContent=` .learning-data-reset-v333{width:100%;flex-basis:100%;margin-top:12px;padding-top:12px;border-top:1px solid rgba(239,68,68,.28)} .learning-data-reset-v333 .reset-title{font-weight:800;margin-bottom:4px} .learning-data-reset",
      "x=document.createElement('div'); box.className='learning-data-reset-v333'; const title=document.createElement('div'); title.className='reset-title';title.textContent='FE QUESTを最初からやり直す'; const help=document.createElement('div'); help.className='reset-help';help.textContent='学習履歴に加えて、試験予定日・学習時間・自動調整などの設定も初期化します。安全のため復旧点と書き出したバックアップだけは残ります。'; const btn=document.createElement('button'); btn.id='resetLearningDataV333';btn.type='button';btn.textContent='FE QUESTを完全に初期化'; btn.addEventListener('click',requestLearningDataResetV333); box.append(title,help,btn);host.append(box); return true; } globalThis.LEARNING_DATA_RESET_SPEC_V333=LEARNING_DATA_RESET_SPEC_V333; globalThis.performLearningDataResetV333=performLearningDataResetV333; globalThis.installLearningDataResetV333=installLearningDataResetV333; if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',installLearningDataResetV333,{once:true}); else installLearningDataResetV333(); // FE QUEST v33",
      "phase-banner\"><b>${p.phase.icon} ${p.phase.name}</b> 今日の推奨 ${total}分。残り${p.days}日では、未完了量を追い掛けず新規学習を段階的に止めます。</div>`; else note.innerHTML=p.hasExam&&!p.expired?`<div class=\"pace-phase-banner\"><b>${p.phase.icon} ${p.phase.name}</b> 今日の目標 ${total}分。試験日が近づくほど、新規学習を減らして復習・科目B・実戦確認へ配分を寄せます。</div>`:'標準学習時間を4つの学習枠に配分します。'; } root.innerHTML=items.map(x=>`<div class=\"planner-row\"><div style=\"font-size:21px\">${x[0]}</div><div class=\"planner-row-copy\"><div class=\"planner-label\">${x[1]}</div></div><b>${x[2]}分</b></div>`).join(''); } function renderPlanFocus(){ const title=document.getElementById('planFocusTitle'),desc=document.getElementById('planFocusDesc'),action=document.getElementById('planFocusAction'),icon=document.getElementById('planFocusIcon'); if(!title||!desc||!action)return; const rec=getDailyRecord(),tasks=ensureTodayPlanSnapshot(); const done=tasks.filter(t=>dailyTaskDone(rec,t)).length,next=tasks.find(t=>!dailyTaskDone(rec,t)); if(next){title.textContent=`${done}/${"
    ],
    "試験予定日": [
      "turn { ok:true, profile:structuredClone(profile), settings:structuredClone(profile.settings||{}), recoveryPointCreated }; } async function requestLearningDataResetV333(){ const first=window.confirm( 'FE QUESTを最初からやり直します。\\n\\n'+ '初期化されるもの：教材の進捗、問題履歴、復習、模試履歴、科目B進捗、XP、ストリーク、試験予定日、1日の学習時間、自動調整、学習計画など\\n\\n'+ '初期化後は試験準備度や必要ペースなども、初回利用時の状態から再計算されます。\\n\\n'+ '安全のため、端末内の復旧点と書き出したバックアップは残ります。誤って初期化した場合だけ復旧センターから戻せます。続けますか？' ); if(!first)return false; const typed=window.prompt('最終確認です。FE QUESTを初期状態へ戻すには「初期化」と入力してください。',''); if(typed!==LEARNING_DATA_RESET_CONFIRM_TEXT_V333){ if(typed!==null)showAppNotice('info','初期化を中止しました','確認文字が一致しなかったため、データは変更していません。'); return false; } const result=await performLearningDataResetV333(); if(!result.ok){ showAppNotice('error','初期化できませんでした','データは変更していません。ほかのFE QUEST画面を閉じてから、もう一度お試しください。'); return false; } window.alert('FE QUESTを初期状態に戻しました。試験予定日や学習時間などの設定もリセットされています。'); location.reload(); retur",
      "tice('info','初期化を中止しました','確認文字が一致しなかったため、データは変更していません。'); return false; } const result=await performLearningDataResetV333(); if(!result.ok){ showAppNotice('error','初期化できませんでした','データは変更していません。ほかのFE QUEST画面を閉じてから、もう一度お試しください。'); return false; } window.alert('FE QUESTを初期状態に戻しました。試験予定日や学習時間などの設定もリセットされています。'); location.reload(); return true; } function installLearningDataResetV333(){ if(document.getElementById('resetLearningDataV333'))return true; const host=document.querySelector('#pwaHealthCard .pwa-health-actions-advanced'); if(!host)return false; if(!document.getElementById('learningDataResetStyleV333')){ const style=document.createElement('style'); style.id='learningDataResetStyleV333'; style.textContent=` .learning-data-reset-v333{width:100%;flex-basis:100%;margin-top:12px;padding-top:12px;border-top:1px solid rgba(239,68,68,.28)} .learning-data-reset-v333 .reset-title{font-weight:800;margin-bottom:4px} .learning-data",
      "nst box=document.createElement('div'); box.className='learning-data-reset-v333'; const title=document.createElement('div'); title.className='reset-title';title.textContent='FE QUESTを最初からやり直す'; const help=document.createElement('div'); help.className='reset-help';help.textContent='学習履歴に加えて、試験予定日・学習時間・自動調整などの設定も初期化します。安全のため復旧点と書き出したバックアップだけは残ります。'; const btn=document.createElement('button'); btn.id='resetLearningDataV333';btn.type='button';btn.textContent='FE QUESTを完全に初期化'; btn.addEventListener('click',requestLearningDataResetV333); box.append(title,help,btn);host.append(box); return true; } globalThis.LEARNING_DATA_RESET_SPEC_V333=LEARNING_DATA_RESET_SPEC_V333; globalThis.performLearningDataResetV333=performLearningDataResetV333; globalThis.installLearningDataResetV333=installLearningDataResetV333; if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',installLearningDataResetV333,{once:true}); else installLearningDataResetV333(); // FE QUE"
    ],
    "試験日": [
      "了換算で ${Math.round(p.currentPace)}分/日`:`完了記録がまだ少ないため、設定中の ${p.baseline}分/日 で試算`; const finish=p.remaining<=0?'主要メニューは完了済み':`今のペースなら ${paceDateText(p.projectedDate)}ごろ完了見込み`; let adj=''; if(p.auto)adj=p.effective>p.baseline ? (p.canRecover?`<b>自動調整中：</b> 標準${p.baseline}分 → 今日${p.effective}分。試験日に間に合わせるため、目標時間を引き上げています。`:`<b>自動調整中：</b> 上限120分まで引き上げても不足します。受験日の見直しも検討してください。`) : `<b>自動調整ON：</b> 今は標準${p.baseline}分のままで十分です。遅れが出ると翌日以降の目標と配分を自動調整します。`; else adj=`自動調整はOFFです。必要ペースは約${Math.ceil(p.required)}分/日です。`; root.innerHTML=`<div class=\"pace-overview ${tone}\"> <div class=\"pace-head\"><div class=\"pace-status-icon\">${icon}</div><div class=\"pace-head-copy\"><div class=\"pace-phase\">${p.phase.icon} ${p.phase.name}</div><div class=\"pace-title\">${title}</div><div class=\"pace-explain\">${current}。${finish}。</div></div></div> <div class=\"pace-metrics\"> <div class=\"pace-metric\"><span>FE QUEST内の残り学習量</span><b>${Math.ceil(p.remaining/60)}時間</b></div> <div class=\"pace-me",
      "b>${Math.ceil(p.required)}分/日</b></div> <div class=\"pace-metric\"><span>現在ペース（完了換算）</span><b>${Math.round(p.currentPace)}分/日</b></div> <div class=\"pace-metric\"><span>今日の目標</span><b>${p.effective}分</b></div> </div> <div class=\"pace-projection\"><b>完了見込み：</b> ${finish}${p.slack>=0?`（試験日の約${p.slack}日前）`:`（試験日より約${Math.abs(p.slack)}日遅れ）`}</div> <div class=\"pace-adjustment\">${adj}</div> ${!p.canRecover&&p.remaining>0?`<div class=\"pace-note\">1日120分で進めた場合でも、最短完了目安は ${paceDateText(p.maxDate)} ごろです。</div>`:''} <div class=\"pace-note\">この判定はFE QUEST内の推奨メニューを終えるための目安で、合格可能性そのものではありません。</div> </div>`; } function renderAllocation(){ const root=document.getElementById('planAllocation');if(!root)return; const total=effectiveStudyMinutes(),p=examPaceStatus(),a=taskAllocation(total); const taper=!!p.taper; const lessonLabel=taper&&p.days<=3?'既習の弱点補強':p.hasExam&&p.days<=30?'弱点学習':'新規学習'; const bossLabel=taper&&p.days===0?'ウォームアップ':p.hasExam&&p.days<=14?'総合チェック':'総",
      "ed)}分/日</b></div> <div class=\"pace-metric\"><span>現在ペース（完了換算）</span><b>${Math.round(p.currentPace)}分/日</b></div> <div class=\"pace-metric\"><span>今日の目標</span><b>${p.effective}分</b></div> </div> <div class=\"pace-projection\"><b>完了見込み：</b> ${finish}${p.slack>=0?`（試験日の約${p.slack}日前）`:`（試験日より約${Math.abs(p.slack)}日遅れ）`}</div> <div class=\"pace-adjustment\">${adj}</div> ${!p.canRecover&&p.remaining>0?`<div class=\"pace-note\">1日120分で進めた場合でも、最短完了目安は ${paceDateText(p.maxDate)} ごろです。</div>`:''} <div class=\"pace-note\">この判定はFE QUEST内の推奨メニューを終えるための目安で、合格可能性そのものではありません。</div> </div>`; } function renderAllocation(){ const root=document.getElementById('planAllocation');if(!root)return; const total=effectiveStudyMinutes(),p=examPaceStatus(),a=taskAllocation(total); const taper=!!p.taper; const lessonLabel=taper&&p.days<=3?'既習の弱点補強':p.hasExam&&p.days<=30?'弱点学習':'新規学習'; const bossLabel=taper&&p.days===0?'ウォームアップ':p.hasExam&&p.days<=14?'総合チェック':'総合チェック'; const items=",
      "e){ if(taper)note.innerHTML=`<div class=\"pace-phase-banner\"><b>${p.phase.icon} ${p.phase.name}</b> 今日の推奨 ${total}分。残り${p.days}日では、未完了量を追い掛けず新規学習を段階的に止めます。</div>`; else note.innerHTML=p.hasExam&&!p.expired?`<div class=\"pace-phase-banner\"><b>${p.phase.icon} ${p.phase.name}</b> 今日の目標 ${total}分。試験日が近づくほど、新規学習を減らして復習・科目B・実戦確認へ配分を寄せます。</div>`:'標準学習時間を4つの学習枠に配分します。'; } root.innerHTML=items.map(x=>`<div class=\"planner-row\"><div style=\"font-size:21px\">${x[0]}</div><div class=\"planner-row-copy\"><div class=\"planner-label\">${x[1]}</div></div><b>${x[2]}分</b></div>`).join(''); } function renderPlanFocus(){ const title=document.getElementById('planFocusTitle'),desc=document.getElementById('planFocusDesc'),action=document.getElementById('planFocusAction'),icon=document.getElementById('planFocusIcon'); if(!title||!desc||!action)return; const rec=getDailyRecord(),tasks=ensureTodayPlanSnapshot(); const done=tasks.filter(t=>dailyTaskDone(rec,t)).length,next=tasks.find(t=>!dailyTaskDone"
    ],
    "今日の計画": [
      " desc.textContent=`復習タイミングの問題が${due}問あります。忘却リスクが高い順に確認します。`; action.textContent='復習を始める →'; action.onclick=()=>startQuiz('review'); return; } if(nextDaily && ['boss','warmup','taperReview'].includes(nextDaily.type)){ icon.textContent=nextDaily.icon||'✅'; kicker.textContent='今日の計画'; title.textContent=nextDaily.title; desc.textContent=`${nextDaily.minutes}分の予定です。今日の学習計画に沿って進めます。`; action.textContent='始める →'; action.onclick=()=>launchDailyTask(nextDaily); return; } const rx=recommendedPrescription(); const meta=prescriptionMeta(rx); icon.textContent=meta.icon||'🎯'; title.textContent=profile.sessions?.length?meta.title:'弱点を10問で確認'; desc.textContent=profile.sessions?.length?meta.desc:'復習待ちはありません。現在の習熟度が低い分野から10問を選びます。'; action.textContent='始める →'; action.onclick=()=>profile.sessions?.length?startRecommendedPrescription():startQuiz('weak'); } function openProblemsHub(){ document.getElementById('problems')?.classList.remove('exerc",
      "reTodayPlanSnapshot(); const pendingLearn=tasks.find(t=>dailyTaskSlot(t)==='learn'&&!dailyTaskDone(rec,t)); if(pendingLearn){ kicker.textContent='今日の学習枠'; icon.textContent=pendingLearn.icon||'📚'; title.textContent=pendingLearn.title; desc.textContent=`${pendingLearn.minutes}分の予定。今日の計画に沿ってここから始めます。`; action.disabled=false;action.textContent='今日の学習を始める →';action.onclick=()=>launchDailyTask(pendingLearn); const isB=pendingLearn.type==='subjectB'||(pendingLearn.type==='finalFocus'&&pendingLearn.focus?.kind==='subjectB'); if(isB){ const b=pendingLearn.type==='subjectB'?{mode:pendingLearn.bmode,id:pendingLearn.bid}:pendingLearn.focus?.b; setCourseSubject('B',false);updateCourseBRecommendedCard(b?.mode); }else{ setCourseSubject('A',false);updateCourseBRecommendedCard(null); } renderCoreCourseMap(document.getElementById('coreCourseSearch')?.value||''); return; } kicker.textContent=(days!=null&&days>=0&&days<=3)?'今日の方針':'今日の",
      "e);updateCourseBRecommendedCard(null); } renderCoreCourseMap(document.getElementById('coreCourseSearch')?.value||''); return; } kicker.textContent=(days!=null&&days>=0&&days<=3)?'今日の方針':'今日の学習枠'; icon.textContent='✅'; title.textContent=(days!=null&&days>=0&&days<=3)?'新しい教材を増やさず、今日の計画を優先':'今日の教材・基礎学習は完了'; desc.textContent=(days!=null&&days>=0&&days<=3)?'試験直前は復習と短い確認を優先します。教材一覧は参照できますが、追加学習はおすすめしません。':'追加で進める必要はありません。復習や演習など、今日の残りはホームから確認できます。'; action.disabled=false;action.textContent='ホームで今日の残りを確認 →';action.onclick=()=>showScreen('home'); updateCourseBRecommendedCard(null); renderCoreCourseMap(document.getElementById('coreCourseSearch')?.value||''); } function coreTopicLearningState(id){ const lessonPct=profile.lessonProgress?.[id]||0; const topic=CORE_A_TOPIC_MAP[id]||CORE_A_CURRICULUM.find(t=>t.id===id); const qs=QUESTION_BANK.filter(q=>q.coreTopicId===id&&isCoreTopicImmediatePracticeQuestion(q)); const stats=qs.map(q=>({q,st:profile.qStats?.[q.i",
      "ent.getElementById('rightDailyAction'); const nextTask=tasks.find(t=>!dailyTaskDone(rec,t)); if(rightProgress)rightProgress.textContent=`${doneTasks.length}/${tasks.length}完了・${doneMinutes}/${total}分`; if(rightNext)rightNext.textContent=nextTask?`次は「${nextTask.title}」・目安${nextTask.minutes}分`:'今日の計画は完了しています。'; if(rightAction){rightAction.disabled=!nextTask;rightAction.textContent=nextTask?'続きから再開 →':'今日の学習は完了 ✓';rightAction.onclick=nextTask?()=>launchDailyTask(nextTask):null;} } function launchDailyTask(t){ if(t.type==='warmup'){ showScreen('problems'); startQuiz('warmup'); return; } if(t.type==='taperReview'){ showScreen('problems'); startQuiz('taperreview'); return; } if(t.type==='review'){ const j=actionableReviewJourneys()[0]; if(j){ startJourneyAction(j.id); }else{ showScreen('problems'); startQuiz('review'); } return; } if(t.type==='lesson'){ startLesson(t.lessonId); return; } if(t.typ",
      "Icon'); if(!title||!desc||!action)return; const rec=getDailyRecord(),tasks=ensureTodayPlanSnapshot(); const done=tasks.filter(t=>dailyTaskDone(rec,t)).length,next=tasks.find(t=>!dailyTaskDone(rec,t)); if(next){title.textContent=`${done}/${tasks.length}完了・次は「${next.title}」`;desc.textContent=`今日の計画の残りを優先します。目安 ${next.minutes}分。`;icon.textContent=next.icon||'📅';action.textContent='次を始める →';action.disabled=false;action.onclick=()=>launchDailyTask(next);} else{title.textContent='今日の計画は完了しています';desc.textContent='追加で詰め込まず、必要なら演習や分析を確認できます。';icon.textContent='✓';action.textContent='ホームへ →';action.disabled=false;action.onclick=()=>showScreen('home');} } function renderPlannerScreen(){ const mins=document.getElementById('studyMinutesSetting'),date=document.getElementById('examDateSetting'),auto=document.getElementById('autoPaceSetting'); if(mins)mins.value=String(profile.settings.studyMinutes||60);if(date)date.value=profile.settings.examDate||'';if(auto)auto.checked=profile.setting"
    ],
    "学習計画": [
      " action.onclick=()=>startQuiz('review'); return; } if(nextDaily && ['boss','warmup','taperReview'].includes(nextDaily.type)){ icon.textContent=nextDaily.icon||'✅'; kicker.textContent='今日の計画'; title.textContent=nextDaily.title; desc.textContent=`${nextDaily.minutes}分の予定です。今日の学習計画に沿って進めます。`; action.textContent='始める →'; action.onclick=()=>launchDailyTask(nextDaily); return; } const rx=recommendedPrescription(); const meta=prescriptionMeta(rx); icon.textContent=meta.icon||'🎯'; title.textContent=profile.sessions?.length?meta.title:'弱点を10問で確認'; desc.textContent=profile.sessions?.length?meta.desc:'復習待ちはありません。現在の習熟度が低い分野から10問を選びます。'; action.textContent='始める →'; action.onclick=()=>profile.sessions?.length?startRecommendedPrescription():startQuiz('weak'); } function openProblemsHub(){ document.getElementById('problems')?.classList.remove('exercise-session-active'); if(problemHub) problemHub.style.display='grid'; if(quizSession) quizSessi",
      " profile:structuredClone(profile), settings:structuredClone(profile.settings||{}), recoveryPointCreated }; } async function requestLearningDataResetV333(){ const first=window.confirm( 'FE QUESTを最初からやり直します。\\n\\n'+ '初期化されるもの：教材の進捗、問題履歴、復習、模試履歴、科目B進捗、XP、ストリーク、試験予定日、1日の学習時間、自動調整、学習計画など\\n\\n'+ '初期化後は試験準備度や必要ペースなども、初回利用時の状態から再計算されます。\\n\\n'+ '安全のため、端末内の復旧点と書き出したバックアップは残ります。誤って初期化した場合だけ復旧センターから戻せます。続けますか？' ); if(!first)return false; const typed=window.prompt('最終確認です。FE QUESTを初期状態へ戻すには「初期化」と入力してください。',''); if(typed!==LEARNING_DATA_RESET_CONFIRM_TEXT_V333){ if(typed!==null)showAppNotice('info','初期化を中止しました','確認文字が一致しなかったため、データは変更していません。'); return false; } const result=await performLearningDataResetV333(); if(!result.ok){ showAppNotice('error','初期化できませんでした','データは変更していません。ほかのFE QUEST画面を閉じてから、もう一度お試しください。'); return false; } window.alert('FE QUESTを初期状態に戻しました。試験予定日や学習時間などの設定もリセットされています。'); location.reload(); return true; } function",
      "profile.settings.examDate=document.getElementById('examDateSetting').value||''; profile.settings.autoPace=!!document.getElementById('autoPaceSetting')?.checked; saveProfile(); rebuildTodayPlanPreservingDone(); renderPlannerScreen();const p=examPaceStatus(); popToast(p.hasExam&&!p.expired?`学習計画を保存しました・未完了分を再編成しました`:'学習計画を保存しました・未完了分を再編成しました ✓'); }); document.getElementById('rebuildTodayPlan')?.addEventListener('click',()=>{ rebuildTodayPlanPreservingDone(); renderPlannerScreen(); popToast('完了済みを残して、今日の未完了分を再編成しました ✓'); }); // Keep all planner UI in sync when profile data changes. const _refreshProfileUI_v10=refreshProfileUI; refreshProfileUI=function(){ _refreshProfileUI_v10(); renderDailyPlan(); renderPlannerScreen(); }; function setPlanDetailsOpen(open){ const screen=document.getElementById('plan'),btn=document.getElementById('planDetailsToggle'); if(!screen||!btn)return; screen.classList.toggle('plan-compact',!open); btn.setAttribute('aria-expanded',St",
      "ocument.getElementById('examDateSetting').value||''; profile.settings.autoPace=!!document.getElementById('autoPaceSetting')?.checked; saveProfile(); rebuildTodayPlanPreservingDone(); renderPlannerScreen();const p=examPaceStatus(); popToast(p.hasExam&&!p.expired?`学習計画を保存しました・未完了分を再編成しました`:'学習計画を保存しました・未完了分を再編成しました ✓'); }); document.getElementById('rebuildTodayPlan')?.addEventListener('click',()=>{ rebuildTodayPlanPreservingDone(); renderPlannerScreen(); popToast('完了済みを残して、今日の未完了分を再編成しました ✓'); }); // Keep all planner UI in sync when profile data changes. const _refreshProfileUI_v10=refreshProfileUI; refreshProfileUI=function(){ _refreshProfileUI_v10(); renderDailyPlan(); renderPlannerScreen(); }; function setPlanDetailsOpen(open){ const screen=document.getElementById('plan'),btn=document.getElementById('planDetailsToggle'); if(!screen||!btn)return; screen.classList.toggle('plan-compact',!open); btn.setAttribute('aria-expanded',String(!!open)); btn.textCo"
    ]
  }
}
